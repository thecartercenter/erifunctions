# Append a timestamped step entry to an operation log's steps list

Append a timestamped step entry to an operation log's steps list

## Usage

``` r
.eri_log_step(steps, step, status = "success", ...)
```
