# Print method for survey_status objects

Renders a one-line-per-form summary using `cli`.

## Usage

``` r
# S3 method for class 'survey_status'
print(x, ...)
```

## Arguments

- x:

  A `survey_status` object.

- ...:

  Ignored.

## Value

`x`, invisibly.
