# Download one Azure file to a local path (native progress bar suppressed unless `progress`).

Download one Azure file to a local path (native progress bar suppressed
unless `progress`).

## Usage

``` r
.eri_blob_read(con, src, dest, overwrite = TRUE, progress = FALSE)
```
