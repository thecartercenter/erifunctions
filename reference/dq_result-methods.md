# S3 methods for dq_result objects

S3 methods for dq_result objects

## Usage

``` r
# S3 method for class 'dq_result'
print(x, ...)

# S3 method for class 'dq_result'
summary(object, ...)
```

## Arguments

- x, object:

  A `dq_result` object returned by
  [`run_dq_checks()`](https://thecartercenter.github.io/erifunctions/reference/run_dq_checks.md).

- ...:

  Unused; included for S3 method compatibility.
