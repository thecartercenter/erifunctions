# Upload one local file to Azure (native progress bar suppressed unless `progress`).

Upload one local file to Azure (native progress bar suppressed unless
`progress`).

## Usage

``` r
.eri_blob_write(con, src, dest, progress = FALSE)
```
