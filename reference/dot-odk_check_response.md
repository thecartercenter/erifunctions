# Check an httr response and return parsed content, or abort on HTTP error

Check an httr response and return parsed content, or abort on HTTP error

## Usage

``` r
.odk_check_response(resp, context = "ODK API request")
```
