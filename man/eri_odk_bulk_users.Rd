% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/odk_bulk_users.R
\name{eri_odk_bulk_users}
\alias{eri_odk_bulk_users}
\title{Manage ODK app users in bulk from a validated CSV}
\usage{
eri_odk_bulk_users(csv_path, con = NULL, dry_run = FALSE)
}
\arguments{
\item{csv_path}{\code{chr} Path to a CSV file with columns
\code{project_id}, \code{form_id}, \code{action}, \code{actor_name}.
Supported actions: \code{"assign"}, \code{"remove"}, \code{"create"}.}

\item{con}{\code{odk_connection} or \code{NULL} ODK connection from \code{\link[=init_odk_connection]{init_odk_connection()}}.
Falls back to \code{ODK_URL} / \code{ODK_TOKEN} environment variables.}

\item{dry_run}{\code{lgl} If \code{TRUE}, run pre-flight only and print what would happen.
No API mutation calls are made.}
}
\value{
A tibble with one row per input row and a \code{result} column (invisibly).
In \code{dry_run} mode returns \code{invisible(NULL)}.
}
\description{
Reads a CSV of user/form actions, runs pre-flight validation against the live
ODK server, then executes all actions. All validation errors are collected and
reported together before any API calls are made.
}
\details{
For \code{"assign"} rows: if the named app user does not yet exist in the project,
they are created automatically before the form assignment is made.
The assignment uses ODK Central role ID 2 (App User / data collection role).

For \code{"remove"} rows: the form assignment is revoked using role ID 2.
The app-user account itself is not deleted.
}
\examples{
\dontrun{
# CSV contents:
# project_id,form_id,action,actor_name
# 7,RiverProspection,assign,Jane Fieldworker
# 7,FlyCollection,remove,Jane Fieldworker

eri_odk_bulk_users("users.csv", dry_run = TRUE)
eri_odk_bulk_users("users.csv")
}
}
\seealso{
Other ODK Central functions: 
\code{\link{download_form_attachments}()},
\code{\link{download_odk_form}()},
\code{\link{eri_odk_deregister}()},
\code{\link{eri_odk_list_registered}()},
\code{\link{eri_odk_purge}()},
\code{\link{eri_odk_register}()},
\code{\link{eri_odk_sync}()},
\code{\link{eri_odk_upload}()},
\code{\link{list_all_odk_app_users}()},
\code{\link{list_odk_forms}()},
\code{\link{list_odk_projects}()},
\code{\link{update_odk_app_user_role}()}
}
\concept{ODK Central functions}
