% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dq_export.R
\name{eri_dq_export}
\alias{eri_dq_export}
\title{Export a DQ flag report to HTML or markdown}
\usage{
eri_dq_export(
  flags,
  file = NULL,
  format = c("html", "md"),
  country = NULL,
  period = NULL
)
}
\arguments{
\item{flags}{\code{tibble} A flags tibble -- must have \code{column}, \code{value},
\code{issue} columns at minimum. \code{sheet} groups rows into sections (omitted if
absent -- everything renders as one table); \code{excel_row}/\code{row} labels the
row (whichever is present, \code{excel_row} preferred); \code{status} and \code{note}
are shown when present.}

\item{file}{\code{chr} or \code{NULL} Output path. If \code{NULL}, writes
\verb{dq-report-<country>-<period>-<date>.<ext>} (falling back to just the
date if \code{country}/\code{period} are \code{NULL}) in the working directory.}

\item{format}{\code{chr} \code{"html"} (default, self-contained, prints cleanly to
PDF from a browser) or \code{"md"} (GitHub-flavoured markdown).}

\item{country}{\code{str} or \code{NULL} Country code, used only to label the report
and default the output filename.}

\item{period}{\code{str} or \code{NULL} Reporting period, used only to label the
report and default the output filename.}
}
\value{
The output file path (invisibly).
}
\description{
Renders a DQ flags tibble -- either the raw \code{flags} from \code{\link[=run_dq_checks]{run_dq_checks()}}
(\code{column}/\code{value}/\code{issue}, one dataset) or the richer per-CMR-measure
tibble from \code{\link[=eri_cmr_dq_report]{eri_cmr_dq_report()}} (adds \code{sheet}, \code{excel_row}, \code{status},
\code{note}) -- to a self-contained file. This is the artifact a DA hands back
to a data source or pastes into an email/Teams thread once DQ checks have
been reviewed, replacing ad hoc \code{\link[=eri_table]{eri_table()}} calls with one consistent
format. Deliberately hand-rolled rather than routed through
\code{\link[=eri_report_html]{eri_report_html()}} (which hard-requires a working Quarto install): see
\code{R/reports_lite.R} for the shared page shell/CSS this shares with
\code{\link[=eri_feedback_report]{eri_feedback_report()}}.
}
\examples{
\dontrun{
schema <- load_dq_schema("dr", "malaria", "surveillance", "aggregate")
res    <- run_dq_checks(extract, schema)
eri_dq_export(res$flags, country = "dr")

flags <- eri_cmr_dq_report("sdn", "202605")
eri_dq_export(flags, country = "sdn", period = "202605")
}
}
\seealso{
\code{\link[=eri_cmr_dq_report]{eri_cmr_dq_report()}} / \code{\link[=run_dq_checks]{run_dq_checks()}} to generate \code{flags},
\code{\link[=eri_dq_review]{eri_dq_review()}} for the interactive triage wrapper that calls this to
print its report.
}
