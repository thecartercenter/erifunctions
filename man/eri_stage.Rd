% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dal.R
\name{eri_stage}
\alias{eri_stage}
\title{Stage intermediate pipeline output into the data/ blob}
\usage{
eri_stage(
  pipeline,
  country,
  disease,
  pattern = NULL,
  overwrite = FALSE,
  projects_con = NULL,
  data_con = NULL
)
}
\arguments{
\item{pipeline}{\code{str} Registered pipeline name. Currently \code{"hsp-mal"}.}

\item{country}{\code{str} Country code (e.g. \code{"dr"}, \code{"ht"}).}

\item{disease}{\code{str} Disease name (e.g. \code{"malaria"}).}

\item{pattern}{\code{str} or \code{NULL} Optional substring filter applied to filenames
before staging (e.g. \code{"2026"} to stage only 2026 files). Default \code{NULL} stages all files.}

\item{overwrite}{\code{logical} Controls behaviour when a file already exists in
\verb{staged/}. \code{FALSE} (default) issues a \code{\link[cli:cli_abort]{cli::cli_warn()}} for each collision
before overwriting — useful for interactive review. \code{TRUE} overwrites
silently — intended for scripted or automated workflows.}

\item{projects_con}{Azure container object for the \code{projects} blob. If \code{NULL}
(default), connects automatically using \code{\link[=get_azure_storage_connection]{get_azure_storage_connection()}}.}

\item{data_con}{Azure container object for the \code{data} blob. If \code{NULL}
(default), connects using \code{ERIFUNCTIONS_DATA_STORAGE_NAME}.}
}
\value{
Invisibly, a character vector of the staged file paths in the \code{data} blob.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Pulls cleaned files from the \code{projects} blob's \verb{intermediate/} folder for a
registered pipeline and copies them into \verb{data/\{country\}/\{disease\}/surveillance/staged/},
ready for analyst review via \code{\link[=eri_approve]{eri_approve()}}.

If any destination file already exists in \verb{staged/}, a warning is issued for
each collision and the file is overwritten.
\subsection{Registered pipelines}{\tabular{lll}{
   Name \tab Project folder \tab Countries \cr
   \code{hsp-mal} \tab health-hsp-malaria-dev \tab \code{"dr"}, \code{"ht"} \cr
}

}
}
\examples{
\dontrun{
eri_stage("hsp-mal", "dr", "malaria")
eri_stage("hsp-mal", "ht", "malaria", pattern = "2026")
eri_stage("hsp-mal", "dr", "malaria", overwrite = TRUE)  # silent, for scripts
}
}
