% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dq.R
\name{eri_dq_schema_submit}
\alias{eri_dq_schema_submit}
\title{Submit a local DQ schema override for a maintainer to fold in}
\usage{
eri_dq_schema_submit(
  country,
  disease,
  data_source = NULL,
  data_type = NULL,
  note = NULL,
  azcontainer = suppressMessages(get_azure_storage_connection(storage_name =
    Sys.getenv("ERIFUNCTIONS_DATA_STORAGE_NAME", unset = "data")))
)
}
\arguments{
\item{country}{\code{str} Country code (e.g. \code{"dr"}, \code{"uga"}).}

\item{disease}{\code{str} Disease (e.g. \code{"malaria"}, \code{"lf"}).}

\item{data_source}{\code{str} The channel: \code{"surveillance"}, \code{"programmatic"},
\code{"research"}.}

\item{data_type}{\code{str} The measure (e.g. \code{"case"}, \code{"treatment"}); optional
for \code{research}.}

\item{note}{\code{str} or \code{NULL} An optional one-line note appended after the
auto-drafted diff (e.g. why the change matters, or which real submission
surfaced it).}

\item{azcontainer}{Azure container object from \code{\link[=get_azure_storage_connection]{get_azure_storage_connection()}}.}
}
\value{
Invisibly, the logged ticket from \code{\link[=eri_feedback]{eri_feedback()}} (\code{NULL} if the
override is identical to upstream, in which case nothing is filed).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Packages a live local schema override (from \code{\link[=eri_dq_schema_edit]{eri_dq_schema_edit()}}) into a
ticket via \code{\link[=eri_feedback]{eri_feedback()}}: the message is an auto-drafted, human-readable
diff against the schema it was forked from (so a maintainer never has to
retype YAML from a prose description), the full override file is attached,
and the four ADR-0012 axes plus the schema's own stem are recorded as
\code{context}. Filed under \code{area = "dq"}.

Submitting does \strong{not} apply the change anywhere else — it only files the
ticket. Folding it in means a maintainer updates the Azure \verb{schemas/}
\code{.yaml} blob directly (\code{\link[=load_dq_schema]{load_dq_schema()}} already prefers the Azure copy
over the bundled one), which takes effect for every DA within minutes, not
at the next package release. Your own local override keeps working
independently (see \code{\link[=eri_dq_schema_status]{eri_dq_schema_status()}}) until it's reset or the
upstream change retires it.
}
\examples{
\dontrun{
eri_dq_schema_edit("sdn", "oncho", "programmatic", "treatment")
# ... edit the file, e.g. widen a range or add a district alias ...
eri_dq_schema_submit("sdn", "oncho", "programmatic", "treatment",
                     note = "Barbar's real submissions use this alias")
}
}
\seealso{
\code{\link[=eri_dq_schema_edit]{eri_dq_schema_edit()}} to create the override being submitted,
\code{\link[=eri_feedback]{eri_feedback()}} for the general ticket log.

Other DQ schema functions: 
\code{\link{eri_dq_schema_edit}()},
\code{\link{eri_dq_schema_path}()},
\code{\link{eri_dq_schema_reset}()},
\code{\link{eri_dq_schema_status}()},
\code{\link{load_dq_schema}()}
}
\concept{DQ schema functions}
