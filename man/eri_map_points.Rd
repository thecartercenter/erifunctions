% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maps.R
\name{eri_map_points}
\alias{eri_map_points}
\title{Point overlay map}
\usage{
eri_map_points(
  shapefile,
  point_data,
  lat_col,
  lon_col,
  fill_col = NULL,
  shape_col = NULL,
  point_size = 2,
  point_shape = 21,
  title = NULL,
  scale_bar = TRUE,
  north_arrow = TRUE
)
}
\arguments{
\item{shapefile}{An \code{sf} polygon object used as the base layer.}

\item{point_data}{Data frame with coordinate columns.}

\item{lat_col}{\code{chr} Latitude column name.}

\item{lon_col}{\code{chr} Longitude column name.}

\item{fill_col}{\code{chr} Column in \code{point_data} used for point fill/colour.
\code{NULL} (default) produces solid points.}

\item{shape_col}{\code{chr} Column in \code{point_data} used for point shape.
\code{NULL} (default) uses a single shape.}

\item{point_size}{\code{num} Point size. Default \code{2}.}

\item{point_shape}{\code{int} Shape number when \code{shape_col = NULL}. Default \code{21}
(filled circle with colour border).}

\item{title}{\code{chr} Map title. Default \code{NULL}.}

\item{scale_bar}{\code{lgl} Add a ggspatial scale bar? Default \code{TRUE}.}

\item{north_arrow}{\code{lgl} Add a ggspatial north arrow? Default \code{TRUE}.}
}
\value{
A ggplot object.
}
\description{
Converts \code{point_data} to an \code{sf} object using \code{lat_col} and \code{lon_col},
overlays it on a base \code{shapefile}, and returns a ggplot. Rows with \code{NA}
coordinates are dropped with a warning (same as \code{eri_spatial_join()}).
}
\examples{
\dontrun{
communes <- eri_spatial_load("ht", level = 2)
eri_map_points(
  communes, tas_results,
  lat_col   = "lat",
  lon_col   = "lon",
  fill_col  = "fts_result",
  title     = "Haiti LF TAS results 2024"
)
}
}
