% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog.R
\name{eri_catalog_register}
\alias{eri_catalog_register}
\title{Register a processed-layer file in the data catalog}
\usage{
eri_catalog_register(
  path,
  country,
  disease,
  data_type,
  layer,
  period = NULL,
  row_count = NULL,
  data_con = NULL
)
}
\arguments{
\item{path}{\code{chr} Blob path of the file (e.g. \code{"dr/malaria/surveillance/processed/2024_W01.parquet"}).}

\item{country}{\code{chr} Country code (e.g. \code{"uga"}).}

\item{disease}{\code{chr} Disease name (e.g. \code{"oncho"}).}

\item{data_type}{\code{chr} Data type (e.g. \code{"surveillance"}, \code{"cmr"}, \code{"odk"}).}

\item{layer}{\code{chr} Storage layer (\code{"raw"}, \code{"staged"}, or \code{"processed"}).}

\item{period}{\code{chr} or \code{NULL} Data period string (e.g. \code{"2024-W01"}, \code{"202405"}).}

\item{row_count}{\code{int} or \code{NULL} Number of rows in the file, if known.}

\item{data_con}{Azure container object for the \verb{data/} blob. If \code{NULL}, connects automatically.}
}
\value{
The registered entry (invisibly).
}
\description{
Adds or updates an entry for the given blob path in \verb{_catalog/data_catalog.yaml}
in the \verb{data/} Azure blob. Existing entries are matched by \code{path} (upsert semantics).
}
\examples{
\dontrun{
eri_catalog_register(
  path      = "uga/oncho/surveillance/processed/2024_W01.parquet",
  country   = "uga",
  disease   = "oncho",
  data_type = "surveillance",
  layer     = "processed",
  period    = "2024-W01"
)
}
}
