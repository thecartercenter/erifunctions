% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dq.R
\name{add_anomaly_pct_change}
\alias{add_anomaly_pct_change}
\title{Flag rows with unusual period-over-period percent change}
\usage{
add_anomaly_pct_change(
  data,
  value_col,
  period_col,
  threshold = 0.5,
  group_cols = NULL,
  year_col = NULL
)
}
\arguments{
\item{data}{A tibble or \code{dq_result} object.}

\item{value_col}{\code{str} Name of the numeric column to check.}

\item{period_col}{\code{str} Name of the column that defines time order within
each group (e.g. \code{"EpiWeek"}, \code{"month"}).}

\item{threshold}{\code{num} Absolute percent change threshold (as a proportion).
Default \code{0.5} flags changes greater than 50\%.}

\item{group_cols}{\code{chr} vector of column names to group by before computing
change (e.g. \code{c("Province_Residence", "disease")}). Default \code{NULL} treats
the whole dataset as one group.}

\item{year_col}{\code{str} or \code{NULL} When \code{period_col} resets each year
(e.g. \code{"EpiWeek"} 1–53), supply the year column so ordering is correct
across year boundaries. Default \code{NULL}.}
}
\value{
The input object with two columns added to the data:
\verb{pct_change_\{value_col\}} (numeric) and \verb{anomaly_pct_change_\{value_col\}}
(logical). If the input is a \code{dq_result}, flagged rows are also appended
to \verb{$flags}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Computes period-over-period percent change for a numeric column and flags
rows whose absolute change exceeds \code{threshold}. Works on a plain tibble or a
\code{dq_result} object returned by \code{\link[=run_dq_checks]{run_dq_checks()}}, enabling chaining:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{run_dq_checks(data, schema) |>
  add_anomaly_pct_change("n_cases", "EpiWeek", group_cols = "Province_Residence")
}\if{html}{\out{</div>}}

When passed a \code{dq_result}, anomaly rows are appended to \verb{$flags} and the
percent-change columns are added to \verb{$data}.
}
\examples{
\dontrun{
agg <- dplyr::count(raw_dr, Year, EpiWeek, Province_Residence, name = "n_cases")
agg_flagged <- add_anomaly_pct_change(agg, "n_cases", "EpiWeek",
                                       group_cols = "Province_Residence",
                                       year_col   = "Year")
}
}
