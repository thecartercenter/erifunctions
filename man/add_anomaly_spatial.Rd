% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dq.R
\name{add_anomaly_spatial}
\alias{add_anomaly_spatial}
\title{Validate admin unit names against a spatial reference}
\usage{
add_anomaly_spatial(data, schema, azcontainer = NULL)
}
\arguments{
\item{data}{A tibble or \code{dq_result} object.}

\item{schema}{Named list returned by \code{\link[=load_dq_schema]{load_dq_schema()}}.}

\item{azcontainer}{Azure container object for the \code{data} blob. If \code{NULL}
(default), connects automatically using \code{ERIFUNCTIONS_DATA_STORAGE_NAME}.
Pass \code{NULL} to skip the Azure download and use only locally cached files.}
}
\value{
For a plain tibble: a flags tibble with columns \code{row}, \code{column},
\code{value}, \code{issue} (same structure as \verb{$flags} in a \code{dq_result}). For a
\code{dq_result}: the same object with mismatches appended to \verb{$flags}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Flags rows where admin unit names in the data do not appear in the canonical
list extracted from a reference shapefile. Checks admin1 and, optionally,
admin2 when a \code{admin2_name_field} is defined in the schema.

The shapefile is downloaded from the Azure \code{data} blob at the path stored in
\code{schema$admin$admin1_spatial} (and \code{admin2_spatial}). If the shapefile is
unavailable or the \code{admin} block is absent from the schema, the check is
skipped with a warning — it never aborts the pipeline.
}
\examples{
\dontrun{
schema <- load_dq_schema("dominican_republic", "malaria")
result <- run_dq_checks(raw_data, schema) |> add_anomaly_spatial(schema)
}
}
