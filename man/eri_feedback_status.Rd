% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feedback.R
\name{eri_feedback_status}
\alias{eri_feedback_status}
\title{Move a feedback ticket through the triage lifecycle}
\usage{
eri_feedback_status(id, status, note = NULL, data_con = NULL)
}
\arguments{
\item{id}{\code{int} The ticket id (as shown by \code{\link[=eri_feedback]{eri_feedback()}} / \code{\link[=eri_feedback_list]{eri_feedback_list()}}).}

\item{status}{\code{chr} The new status. One of submitted, planned, in_progress, fixed, declined.}

\item{note}{\code{chr} or \code{NULL} An optional one-line note recorded with the transition
(e.g. a PR number or a reason for \code{declined}).}

\item{data_con}{Azure container object for the \verb{data/} blob. If \code{NULL}, connects automatically.}
}
\value{
The updated ticket (invisibly), as a named list (including its \code{history}).
}
\description{
Updates the \code{status} of one ticket in \verb{_feedback/feedback_log.yaml} and records
an audit-trail entry of the transition (from, to, who, when, and an optional
note). This is the triage side of the feedback log (ADR-0014): file a ticket
with \code{\link[=eri_feedback]{eri_feedback()}}, then move it as you work it — typically
\code{submitted} -> \code{planned} -> \code{in_progress} -> \code{fixed} (or \code{declined}).
}
\details{
The change records the \strong{verified} signed-in actor (ADR-0003) and is
concurrency-safe (ADR-0002). The status is validated against the controlled
lifecycle; an unknown id aborts without writing.
}
\examples{
\dontrun{
eri_feedback_status(142, "planned")
eri_feedback_status(142, "fixed", note = "shipped in #251")
eri_feedback_status(7, "declined", note = "works as intended")
}
}
\seealso{
\code{\link[=eri_feedback]{eri_feedback()}} to file, \code{\link[=eri_feedback_board]{eri_feedback_board()}} to summarise.
}
