% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/teams.R
\name{get_teams_connection}
\alias{get_teams_connection}
\title{Connect to Microsoft Teams via the Graph API}
\usage{
get_teams_connection(token = NULL)
}
\arguments{
\item{token}{A pre-obtained bearer token string. Returned as-is.}
}
\value{
A bearer token string, or \code{NULL} with a warning if auth fails.
}
\description{
Retrieves a bearer token for sending messages through the Microsoft Graph
API. Checks \code{ERIFUNCTIONS_TEAMS_TOKEN} first; if absent, attempts the OAuth
device-code flow using the app registered in \code{ERIFUNCTIONS_APP_ID}.
}
\details{
On machines where conditional access blocks browser or device-code flows,
use an incoming webhook instead: set \code{ERIFUNCTIONS_TEAMS_WEBHOOK} and call
\code{eri_teams_send()} directly without a token.
}
