% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cutover.R
\name{eri_cutover_status}
\alias{eri_cutover_status}
\title{Report a stream's cutover readiness from the ledger}
\usage{
eri_cutover_status(
  country,
  disease,
  data_source,
  data_type = NULL,
  n = 3,
  data_con = NULL
)
}
\arguments{
\item{country, disease, data_source}{\code{chr} The stream's axes.}

\item{data_type}{\code{chr} or \code{NULL} The measure, where it splits the stream.}

\item{n}{\code{int} Consecutive equivalent periods required for eligibility. Default \code{3}.}

\item{data_con}{Azure container for the \verb{data/} blob. If \code{NULL}, connects automatically.}
}
\value{
Invisibly, a list with \code{eligible} (lgl), \code{streak} (int), \code{n}, and
\code{periods} (a tibble of \code{period}, \code{equivalent}, the delta counts, and \code{recorded_at},
in checked order).
}
\description{
Reads \verb{_cutover/cutover_log.yaml}, takes the most recent entry per \code{period} for
the stream, and computes the \strong{streak}: the number of consecutive most-recent
periods that are \code{equivalent} (ADR-0015). A stream is \emph{eligible} for cutover
when the streak reaches \code{n}. Periods are ordered by the \strong{data \code{period}} (which
for a stream uses one consistent, lexically-sortable label), and re-checking a
period updates its standing — so backfilling an earlier period is handled
correctly.
}
\examples{
\dontrun{
eri_cutover_status("uga", "oncho", "programmatic", data_type = "treatment")
}
}
\seealso{
\code{\link[=eri_cutover_check]{eri_cutover_check()}} to record a period.
}
