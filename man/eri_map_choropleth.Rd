% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maps.R
\name{eri_map_choropleth}
\alias{eri_map_choropleth}
\title{Choropleth map from a shapefile and data frame}
\usage{
eri_map_choropleth(
  shapefile,
  fill_data,
  fill_col,
  admin_col,
  title = NULL,
  fill_label = NULL,
  scale_bar = TRUE,
  north_arrow = TRUE
)
}
\arguments{
\item{shapefile}{An \code{sf} polygon object (e.g. from \code{eri_spatial_load()}).}

\item{fill_data}{A data frame with the values to map.}

\item{fill_col}{\code{chr} Column in \code{fill_data} used for fill aesthetics.}

\item{admin_col}{\code{chr} Column present in BOTH \code{shapefile} and \code{fill_data}
used to join them (e.g. \code{"adm2_name"}).}

\item{title}{\code{chr} Map title. Default \code{NULL} (no title).}

\item{fill_label}{\code{chr} Legend title. Default \code{NULL} (uses \code{fill_col}).}

\item{scale_bar}{\code{lgl} Add a ggspatial scale bar? Default \code{TRUE}.}

\item{north_arrow}{\code{lgl} Add a ggspatial north arrow? Default \code{TRUE}.}
}
\value{
A ggplot object.
}
\description{
Joins \code{fill_data} to \code{shapefile} by \code{admin_col}, applies \code{eri_plot_theme("map")},
and returns a ggplot. The fill variable can be continuous or discrete; pass a
\verb{scale_fill_*} layer to customise.
}
\examples{
\dontrun{
communes <- eri_spatial_load("ht", level = 2)
eri_map_choropleth(
  communes, case_summary,
  fill_col  = "n_cases",
  admin_col = "adm2_name",
  title     = "Haiti malaria cases 2024"
)
}
}
