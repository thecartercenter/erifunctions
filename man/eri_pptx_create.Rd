% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reports_pptx.R
\name{eri_pptx_create}
\alias{eri_pptx_create}
\title{Create an ERI-branded PowerPoint presentation}
\usage{
eri_pptx_create(template = NULL)
}
\arguments{
\item{template}{Optional character path to a custom \code{.pptx} template.
Defaults to the bundled Carter Center template
(\code{inst/templates/eri_template.pptx}).}
}
\value{
An \code{officer} rpptx object.
}
\description{
Loads the Carter Center branded PPTX template bundled with the package and
returns an \code{\link[officer:read_pptx]{officer::read_pptx()}} object. Use the returned object with
\verb{eri_pptx_add_*()} functions to build up the presentation slide by slide,
then write to disk with \code{\link[=eri_pptx_save]{eri_pptx_save()}}.
}
\examples{
\dontrun{
pptx <- eri_pptx_create()
pptx <- eri_pptx_add_title(pptx, "Hispaniola Malaria 2024",
                            subtitle = "Annual Programme Report")
pptx <- eri_pptx_add_table(pptx, summary_df, title = "Case summary")
eri_pptx_save(pptx, "outputs/malaria_report.pptx")
}
}
