% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dq.R
\name{add_anomaly_gaps}
\alias{add_anomaly_gaps}
\title{Flag missing time periods in surveillance data}
\usage{
add_anomaly_gaps(
  data,
  period_col,
  period_type = c("week", "month"),
  group_cols = NULL,
  year_col = NULL
)
}
\arguments{
\item{data}{A tibble or \code{dq_result} object.}

\item{period_col}{\code{str} Column containing the period value (integer week 1–53
or month 1–12).}

\item{period_type}{\code{str} One of \code{"week"} or \code{"month"}.}

\item{group_cols}{\code{chr} vector of columns to check for gaps within each group
(e.g. \code{c("Province_Residence")}). Default \code{NULL} checks the full dataset.}

\item{year_col}{\code{str} or \code{NULL} Column containing the year. Required when
\code{period_type = "week"} or \code{"month"} to detect cross-year gaps.}
}
\value{
A tibble of missing periods with columns \code{year} (if \code{year_col}
supplied), \code{period}, any \code{group_cols}, and \code{issue = "structural_gap"}. If
the input is a \code{dq_result}, missing-period rows are also appended to
\verb{$flags} (with \code{row = NA}). Returns an empty tibble when no gaps are found.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Identifies gaps in a time series by inferring the full expected sequence of
periods between the observed minimum and maximum, then returning rows for any
missing periods. Works on a plain tibble or a \code{dq_result} object.

Supports two period types:
\itemize{
\item \code{"week"} — expects contiguous integers 1–53 within each year. A gap at the
year boundary (week 52/53 → week 1 of the next year) is handled correctly.
\item \code{"month"} — expects contiguous integers 1–12 within each year.
}
}
\examples{
\dontrun{
gaps <- add_anomaly_gaps(agg_data, "EpiWeek", "week",
                          group_cols = "Province_Residence", year_col = "Year")
}
}
