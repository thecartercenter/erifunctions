% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{eri_simulate_check}
\alias{eri_simulate_check}
\title{Confirm the cutover comparison catches injected divergence}
\usage{
eri_simulate_check(
  reference,
  by,
  types = c("missing", "outlier", "negative", "typo", "drop"),
  n = 1L,
  seed = NULL,
  tolerance = 0
)
}
\arguments{
\item{reference}{A clean data frame to perturb and compare against.}

\item{by}{\code{chr} Key column(s) uniquely identifying a row. Anomalies are kept
off these so the divergence shows up as detectable row/value deltas.}

\item{types}{\code{chr} Anomaly types to inject (see \code{\link[=eri_inject_anomalies]{eri_inject_anomalies()}}).
Defaults to all except \code{duplicate}. Passing \code{duplicate} is an error.}

\item{n}{\code{int} Anomalies per type. Default \code{1}.}

\item{seed}{\code{int} or \code{NULL} Optional RNG seed for a reproducible run.}

\item{tolerance}{\code{num} Numeric tolerance passed to \code{\link[=eri_compare]{eri_compare()}}. Default \code{0}.}
}
\value{
Invisibly, a list with \code{detected} (lgl — did the comparison flag a
divergence?), \code{injected} (the anomaly log from \code{\link[=eri_inject_anomalies]{eri_inject_anomalies()}}), and
\code{comparison} (the \code{\link[=eri_compare]{eri_compare()}} result, to inspect which deltas were caught).
}
\description{
Ties the Phase-3 simulation harness together: inject known anomalies into a
clean \code{reference} dataset (in the value columns, off the join keys) and confirm
\code{\link[=eri_compare]{eri_compare()}} — run with the cutover standard — flags the result as \strong{not
equivalent}. Use it to build confidence that the cutover gate
(\code{\link[=eri_cutover_check]{eri_cutover_check()}}) would catch a real divergence before relying on it.
}
\details{
\code{duplicate} is excluded from the default \code{types}: a duplicate key cannot be
reconciled per-cell, so it makes the keyed comparison abort rather than report
a delta (that is a uniqueness check, separate from reconciliation).
}
\examples{
clean <- data.frame(id = 1:8, cases = c(5, 8, 3, 6, 9, 4, 7, 2), site = letters[1:8])
sim <- eri_simulate_check(clean, by = "id", n = 2, seed = 1)
sim$detected            # TRUE — eri_compare flagged the injected anomalies
sim$comparison$values   # the per-cell mismatches it caught
}
\seealso{
\code{\link[=eri_inject_anomalies]{eri_inject_anomalies()}} to dirty data, \code{\link[=eri_compare]{eri_compare()}} to reconcile,
\code{\link[=eri_cutover_check]{eri_cutover_check()}} for the real gate.
}
