% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi.R
\name{eri_case_summary}
\alias{eri_case_summary}
\title{Summarise case data by grouping columns}
\usage{
eri_case_summary(
  data,
  group_cols,
  start = NULL,
  end = NULL,
  date_col = NULL,
  count_col = NULL
)
}
\arguments{
\item{data}{A data frame or tibble of case records.}

\item{group_cols}{\code{chr} vector of columns to group by (e.g. \code{c("country", "year")}).}

\item{start}{\code{Date} or \code{NULL}. If supplied, keeps rows where \code{date_col >= start}.}

\item{end}{\code{Date} or \code{NULL}. If supplied, keeps rows where \code{date_col <= end}.}

\item{date_col}{\code{chr} or \code{NULL}. Required when \code{start} or \code{end} is specified.
The column to filter on.}

\item{count_col}{\code{chr} or \code{NULL}. If \code{NULL}, counts rows. If a column name,
sums that column.}
}
\value{
A tibble with \code{group_cols} plus a \code{n_cases} column.
}
\description{
Aggregates a case data frame by \code{group_cols}, optionally filtering to a date
range. If \code{count_col} is \code{NULL} (case-level data), counts rows. If \code{count_col}
is specified (pre-aggregated data), sums that column.
}
\examples{
\dontrun{
eri_case_summary(
  case_data,
  group_cols = c("country", "year"),
  start      = as.Date("2024-01-01"),
  end        = as.Date("2024-12-31"),
  date_col   = "sample_date"
)
}
}
