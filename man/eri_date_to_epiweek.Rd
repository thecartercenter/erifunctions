% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi.R
\name{eri_date_to_epiweek}
\alias{eri_date_to_epiweek}
\title{Convert a Date to a CDC epiweek number}
\usage{
eri_date_to_epiweek(date, week_start = "Sunday")
}
\arguments{
\item{date}{A \code{Date} vector (or character coercible to Date).}

\item{week_start}{\code{chr} \code{"Sunday"} (CDC default) or \code{"Monday"} (ISO).}
}
\value{
An integer vector of epiweek numbers (1–53).
}
\description{
Returns the epidemiological week number (1–53) for each date. The inverse of
\code{\link[=eri_epiweek_date]{eri_epiweek_date()}}. Uses CDC Sunday-start convention by default (matching
DR and Haiti surveillance data); pass \code{week_start = "Monday"} for ISO weeks.
}
\details{
Dates that fall in an epiweek belonging to a different calendar year (e.g.
Dec 31 in CDC epiweek 1 of the following year) return the correct week number
for that epiweek. Use \code{lubridate::epiyear()} / \code{lubridate::isoyear()} to
obtain the corresponding epi year when needed.
}
\examples{
\dontrun{
eri_date_to_epiweek(as.Date("2024-01-07"))   # 1
eri_date_to_epiweek(as.Date("2024-12-29"))   # 52

# Add epiweek to a case line list
cases |> dplyr::mutate(epiweek = eri_date_to_epiweek(sample_date))
}
}
