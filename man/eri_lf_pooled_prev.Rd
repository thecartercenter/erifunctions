% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi_lf.R
\name{eri_lf_pooled_prev}
\alias{eri_lf_pooled_prev}
\title{Pooled prevalence estimator for LF antigen surveys}
\usage{
eri_lf_pooled_prev(npos, npool, pool_size, by = NULL)
}
\arguments{
\item{npos}{\code{num} Number of positive pools.}

\item{npool}{\code{num} Total number of pools tested.}

\item{pool_size}{\code{num} Number of individuals per pool.}

\item{by}{\code{chr} vector or \code{NULL}. Column name(s) in the enclosing data frame
(for use inside \code{dplyr::mutate()}) or a plain numeric vector equal in
length to \code{npos}. When \code{NULL} (default), returns a single scalar.}
}
\value{
A numeric scalar (ungrouped) or a tibble with \code{by} columns plus
\code{pooled_prev} (grouped).
}
\description{
Implements the standard formula for estimating individual prevalence from
pooled test results: \code{1 - ((1 - npos/npool)^(1/pool_size))}.
}
\details{
When \code{by} is supplied the function returns one prevalence estimate per group
(weighted mean of pool-level estimates, weighted by pool count). Matches the
formula in \code{pooled_prev.R}.
}
\examples{
\dontrun{
# Scalar
eri_lf_pooled_prev(npos = 3, npool = 100, pool_size = 5)

# Grouped tibble
tas_data |>
  dplyr::group_by(commune) |>
  dplyr::summarise(
    npos      = sum(fts_result == "Positive"),
    npool     = dplyr::n(),
    pool_size = mean(pool_size)
  ) |>
  dplyr::mutate(prev = eri_lf_pooled_prev(npos, npool, pool_size))
}
}
