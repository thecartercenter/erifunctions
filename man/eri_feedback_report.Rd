% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feedback.R
\name{eri_feedback_report}
\alias{eri_feedback_report}
\title{Write a weekly feedback report (HTML or markdown)}
\usage{
eri_feedback_report(
  file = NULL,
  format = c("html", "md"),
  since_days = 7,
  data_con = NULL
)
}
\arguments{
\item{file}{\code{chr} or \code{NULL} Output path. If \code{NULL}, writes
\verb{feedback-report-<date>.<ext>} in the working directory.}

\item{format}{\code{chr} \code{"html"} (default, self-contained, open in a browser) or
\code{"md"} (GitHub-flavoured markdown).}

\item{since_days}{\code{num} The digest window in days. Default \code{7} (a weekly report).}

\item{data_con}{Azure container object for the \verb{data/} blob. If \code{NULL}, connects automatically.}
}
\value{
The output file path (invisibly).
}
\description{
Renders the feedback backlog from \verb{_feedback/feedback_log.yaml} to a
self-contained file: a status \strong{board}, then a weekly digest — \strong{new} tickets
filed within \code{since_days}, tickets \strong{closed} (fixed/declined) within
\code{since_days} with their closing note, and the \strong{open} backlog in lifecycle
order. Built for a quick standing review so the team stays current (ADR-0014).
}
\examples{
\dontrun{
eri_feedback_report()                       # feedback-report-<today>.html
eri_feedback_report(format = "md", since_days = 14)
}
}
\seealso{
\code{\link[=eri_feedback_board]{eri_feedback_board()}} for the console summary, \code{\link[=eri_feedback_status]{eri_feedback_status()}} to triage.
}
