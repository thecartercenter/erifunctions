% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sharepoint.R
\name{eri_sharepoint_read}
\alias{eri_sharepoint_read}
\title{Read a file from SharePoint into R}
\usage{
eri_sharepoint_read(site, file_path, ...)
}
\arguments{
\item{site}{A \code{ms_site} object from \code{\link[=eri_sharepoint_connect]{eri_sharepoint_connect()}}.}

\item{file_path}{\code{chr} Path to the file within the document library
(e.g. \code{"Shared Documents/Malaria/2024/ht_weekly.xlsx"}).}

\item{...}{Additional arguments passed to the underlying read function
(e.g. \code{sheet} for Excel files).}
}
\value{
A tibble, data frame, or character path depending on file type.
}
\description{
Downloads a file from a SharePoint document library to a temporary location
and reads it into R. The format is detected from the file extension:
\itemize{
\item \code{.xlsx} / \code{.xls} -- \code{readxl::read_excel()}
\item \code{.csv} -- \code{readr::read_csv()}
\item \code{.parquet} -- \code{arrow::read_parquet()}
\item \code{.rds} -- \code{readr::read_rds()}
\item Other -- returns the local temp path as a character string
}
}
\examples{
\dontrun{
site <- eri_sharepoint_connect("https://cartercenter.sharepoint.com/sites/ERI")
df <- eri_sharepoint_read(site, "Shared Documents/Malaria/2024/ht_weekly.xlsx")
}
}
