% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dq.R
\name{eri_dq_schema_status}
\alias{eri_dq_schema_status}
\title{List local DQ schema overrides}
\usage{
eri_dq_schema_status(
  azcontainer = suppressMessages(get_azure_storage_connection(storage_name =
    Sys.getenv("ERIFUNCTIONS_DATA_STORAGE_NAME", unset = "data")))
)
}
\arguments{
\item{azcontainer}{Azure container object from \code{\link[=get_azure_storage_connection]{get_azure_storage_connection()}}.
Pass \code{NULL} to check staleness against only the bundled copies.}
}
\value{
A tibble with columns \code{stem}, \code{forked_at}, \code{forked_by},
\code{base_source}, \code{status} (\code{"active"}, \code{"stale (will be retired on next load)"},
or \code{"unknown (upstream unreachable)"}). Zero rows if there are no overrides.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Lists every local override created by \code{\link[=eri_dq_schema_edit]{eri_dq_schema_edit()}}, with its age
and whether it is still active or has gone stale (the upstream schema
changed since it was forked -- it will be retired automatically the next
time \code{\link[=load_dq_schema]{load_dq_schema()}} resolves it). Read-only: unlike a real schema load,
checking status never itself retires a stale override.
}
\examples{
\dontrun{
eri_dq_schema_status()
}
}
