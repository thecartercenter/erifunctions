% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feedback.R
\name{eri_feedback}
\alias{eri_feedback}
\title{Log a piece of feedback to the shared ticket log}
\usage{
eri_feedback(message, area = "general", data_con = NULL)
}
\arguments{
\item{message}{\code{chr} The feedback itself. A single non-empty string.}

\item{area}{\code{chr} Which part of the system this is about. \code{"general"} (default)
for system-wide feedback, or a specific section — suggested values:
ingest, dq, catalog, query, odk, cmr, reporting, research, spatial, auth, docs, other. Free text
is accepted; the value is lower-cased.}

\item{data_con}{Azure container object for the \verb{data/} blob. If \code{NULL}, connects automatically.}
}
\value{
The logged ticket (invisibly), as a named list.
}
\description{
Appends a ticket to \verb{_feedback/feedback_log.yaml} in the \verb{data/} Azure blob —
the team's lightweight internal backlog. Use it to flag anything: a bug, a
rough edge, a wish, or a general comment, either about the system as a whole
(\code{area = "general"}) or about a specific part of it (e.g. \code{area = "odk"}).
}
\details{
Each ticket records \strong{who} filed it (the verified signed-in identity, not a
self-declared name — ADR-0003) and \strong{when}, and is given an auto-incrementing
id. Writes are concurrency-safe (ADR-0002), so two people filing at once never
clobber each other. New tickets start at \code{status = "submitted"}; moving a
ticket through triage (\code{planned}, \code{fixed}, ...) is handled by the separate
tracking workflow, not by this function.
}
\examples{
\dontrun{
# System-wide feedback
eri_feedback("The onboarding guide's Week 1 felt too fast.")

# Feedback about a specific section
eri_feedback("ODK sync timed out on the big LF form.", area = "odk")
}
}
\seealso{
\code{\link[=eri_feedback_list]{eri_feedback_list()}} to read the backlog.
}
