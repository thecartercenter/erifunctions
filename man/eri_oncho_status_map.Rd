% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi_oncho.R
\name{eri_oncho_status_map}
\alias{eri_oncho_status_map}
\title{Choropleth map of OEPA oncho program status by focus}
\usage{
eri_oncho_status_map(
  shapefile,
  status_data,
  eu_col,
  status_col,
  title = NULL,
  scale_bar = TRUE,
  north_arrow = TRUE
)
}
\arguments{
\item{shapefile}{An sf object with one row per evaluation unit.}

\item{status_data}{A data frame with at least \code{eu_col} and \code{status_col}.}

\item{eu_col}{Name of the evaluation-unit join key in both \code{shapefile} and
\code{status_data}.}

\item{status_col}{Name of the column in \code{status_data} containing the program
status string.}

\item{title}{Optional map title.}

\item{scale_bar}{Logical; add a scale bar via \code{ggspatial} (default \code{TRUE}).}

\item{north_arrow}{Logical; add a north arrow via \code{ggspatial} (default
\code{TRUE}).}
}
\value{
A \code{ggplot} object.
}
\description{
Joins \code{status_data} to \code{shapefile} on \code{eu_col} and produces a filled
choropleth using the \code{oncho.status} colour scheme. The status column is
coerced to a factor ordered by \code{eri_oncho_program_levels()} so the legend
always renders in the correct progression.
}
\examples{
\dontrun{
eri_oncho_status_map(focus_sf, status_df, eu_col = "focus", status_col = "status")
}
}
