% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmr.R
\name{eri_stage_cmr}
\alias{eri_stage_cmr}
\title{Stage CMR monthly report files into the data/ blob}
\usage{
eri_stage_cmr(
  country,
  period = NULL,
  overwrite = FALSE,
  projects_con = NULL,
  data_con = NULL
)
}
\arguments{
\item{country}{\code{str} Three-letter country code (e.g. \code{"uga"}, \code{"eth"}).
Must be registered in the \code{"rb-expansion"} pipeline.}

\item{period}{\code{str} or \code{NULL} Six-digit period string matching the source
folder name (e.g. \code{"202603"}). Default \code{NULL} uses the most recent period.}

\item{overwrite}{\code{logical} If \code{FALSE} (default), warns before overwriting an
existing staged file. If \code{TRUE}, overwrites silently (for scripted runs).}

\item{projects_con}{Azure container for the \code{projects} blob. \code{NULL} connects
automatically via \code{\link[=get_azure_storage_connection]{get_azure_storage_connection()}}.}

\item{data_con}{Azure container for the \code{data} blob. \code{NULL} connects using
\code{ERIFUNCTIONS_DATA_STORAGE_NAME}.}
}
\value{
Invisibly, a character vector of the staged file paths in the \code{data} blob.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Pulls CMR Excel files from the \code{projects} blob's
\verb{raw/filled_templates/\{country\}/\{period\}/} folder and copies them into
\verb{data/\{country\}/rblf/cmr/staged/}, ready for analyst review via
\code{\link[=eri_approve]{eri_approve()}}.

If \code{period} is \code{NULL}, the most recent period folder (by \code{YYYYMM} name) is
selected automatically and reported to the console. If any destination file
already exists in \verb{staged/}, a warning is issued and the file is overwritten.
}
\examples{
\dontrun{
eri_stage_cmr("uga", "202603")
eri_stage_cmr("nga")  # auto-selects most recent period
}
}
\seealso{
Other CMR pipeline functions: 
\code{\link{eri_approve_cmr}()},
\code{\link{eri_cmr_dq_report}()},
\code{\link{eri_cmr_last_plan}()},
\code{\link{eri_ingest_cmr}()},
\code{\link{eri_split_cmr}()}
}
\concept{CMR pipeline functions}
