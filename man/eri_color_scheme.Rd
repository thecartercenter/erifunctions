% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style.R
\name{eri_color_scheme}
\alias{eri_color_scheme}
\title{ERI standard colour schemes}
\usage{
eri_color_scheme(type)
}
\arguments{
\item{type}{\code{chr} Scheme name. One of:
\itemize{
\item \code{"malaria.incidence"} — white / light-green / yellow / red (0, <1, 1–10, >=10 per 1 000)
\item \code{"lf.status"} — 5-level LF programme status (Non-endemic → PTS TAS-3)
\item \code{"oncho.status"} — 5-level OEPA oncho status (Non-endemic → Verified free)
\item \code{"activities"} — Completed (green) / Not completed (red)
\item \code{"dq.flag"} — pass (grey) / warning (orange) / fail (red)
}}
}
\value{
A named character vector of hex colour codes. Names are the
category labels; values are hex colours.
}
\description{
Returns a named character vector of hex colours for a given programme or
purpose. Designed to be passed directly to \code{scale_fill_manual(values = ...)}.
}
\examples{
\dontrun{
ggplot(...) +
  geom_sf(aes(fill = incidence_class)) +
  scale_fill_manual(values = eri_color_scheme("malaria.incidence"))
}
}
