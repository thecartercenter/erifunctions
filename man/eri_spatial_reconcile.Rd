% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{eri_spatial_reconcile}
\alias{eri_spatial_reconcile}
\title{Reconcile free-text place names to canonical admin units}
\usage{
eri_spatial_reconcile(
  data,
  loc_cols,
  shapefile,
  admin_cols,
  country_name = NULL,
  method = "osm",
  max_dist = 0L,
  status_col = "reconcile_status",
  coord_cols = c("longitude", "latitude"),
  ...
)
}
\arguments{
\item{data}{A data frame or tibble containing the free-text place-name columns.}

\item{loc_cols}{\code{chr} vector of free-text column names, ordered \strong{finest to
coarsest} (e.g. \code{c("loc", "mun", "prov")}). Used both to match and to build
the geocoding address (finest first).}

\item{shapefile}{An admin-boundary \code{sf} object, e.g. from \code{\link[=eri_spatial_load]{eri_spatial_load()}}.}

\item{admin_cols}{\code{chr} vector of canonical name columns in \code{shapefile},
\strong{parallel to \code{loc_cols}} (same length, same finest-to-coarsest order). This
ordering is load-bearing: passing the columns coarsest-first silently produces
wrong matches.}

\item{country_name}{\code{chr} Country name appended to each geocoding address
(e.g. \code{"Dominican Republic"}), improving geocoder accuracy. Optional.}

\item{method}{\code{chr} Geocoding service passed to \code{\link[tidygeocoder:geocode]{tidygeocoder::geocode()}}
(e.g. \code{"osm"}, \code{"google"}). \code{NULL} disables geocoding (match-only). Default \code{"osm"}.}

\item{max_dist}{\code{int} Maximum edit distance for an approximate match on the
finest level. \code{0} (default) requires an exact normalized match.}

\item{status_col}{\code{chr} Name of the status column added to the result.
Default \code{"reconcile_status"}; values are \code{"matched"}, \code{"geocoded"}, or
\code{"unresolved"}.}

\item{coord_cols}{\code{chr} length-2 names for the longitude and latitude columns
added to the result. Default \code{c("longitude", "latitude")}. Populated for any
row sent to the geocoder, regardless of its final status (so geocoded points
that fall outside every polygon still record their coordinates).}

\item{...}{Passed to \code{\link[tidygeocoder:geocode]{tidygeocoder::geocode()}} (e.g. \code{min_time}, \code{api_options}).}
}
\value{
A tibble: \code{data} with \code{loc_cols} replaced by their canonical values
where reconciled (originals kept where unresolved), plus the two \code{coord_cols}
and \code{status_col}.
}
\description{
A thin, opt-in \strong{data-sourcing} helper that maps messy, free-text locality
names in incoming data to the canonical admin units in an authoritative
boundary \code{sf} object (from \code{\link[=eri_spatial_load]{eri_spatial_load()}}). It does the
name-reconciliation step many studies do by hand before analysis -- it is
\strong{not} an analysis tool (matching/windowing/modelling stay in the research
repo; see ADR-0006).
}
\details{
The reconciliation runs in two passes (per issue #134):
\enumerate{
\item \strong{Match first.} Free-text names are normalized (lower-cased, accent- and
punctuation-stripped, whitespace-squished) and matched against the
canonical names. Coarser levels must match exactly; the finest level may
match approximately when \code{max_dist > 0} (Levenshtein distance via
\code{\link[utils:adist]{utils::adist()}}). Matched rows are \strong{not} geocoded.
\item \strong{Geocode the residual.} Rows that don't match are geocoded from an
address built from \code{loc_cols} (+ \code{country_name}), then assigned a canonical
admin unit by point-in-polygon via \code{\link[=eri_spatial_join]{eri_spatial_join()}}. Set
\code{method = NULL} to skip geocoding entirely (match-only).
}

Only the place-name address strings are sent to the geocoder; no data records
leave the machine. The \code{"google"} method is the most accurate but requires an
API key (\code{GOOGLEGEOCODE_API_KEY}) and is billed per call; the default
\code{"osm"} (Nominatim) needs no key. See \code{\link[tidygeocoder:geocode]{tidygeocoder::geocode()}}.
}
\examples{
\dontrun{
dr_loc <- eri_spatial_load("dr", level = 4, cache = TRUE)
incidence <- eri_spatial_reconcile(
  incidence,
  loc_cols     = c("loc", "mun", "prov"),
  shapefile    = dr_loc,
  admin_cols   = c("adm4_name", "adm3_name", "adm2_name"),
  country_name = "Dominican Republic",
  method       = "google",  # needs GOOGLEGEOCODE_API_KEY
  max_dist     = 1
)
table(incidence$reconcile_status)
}
}
