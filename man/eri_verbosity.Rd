% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/console.R
\name{eri_verbosity}
\alias{eri_verbosity}
\title{Control how much erifunctions prints to the console}
\usage{
eri_verbosity(level)
}
\arguments{
\item{level}{\code{chr} Either \code{"full"} (default; chatty) or \code{"quiet"} (terse). Omit to read the
current level instead of setting it.}
}
\value{
The verbosity level, invisibly when setting.
}
\description{
By default erifunctions narrates each step it takes -- confirmations, checklists, and progress
bars -- so you can see what it is doing. If you prefer a terser console, switch to \code{"quiet"}:
headline results, warnings, and errors are still shown, but the step-by-step chatter is hidden.
}
\details{
Set it for a whole project by adding \code{options(erifunctions.verbosity = "quiet")} to the
project's \code{.Rprofile}, or for one session by calling this function. The \code{ERIFUNCTIONS_VERBOSITY}
environment variable is also honoured (useful in CI).
}
\examples{
eri_verbosity()          # read the current level
\dontrun{
eri_verbosity("quiet")   # terser console for the rest of the session
eri_verbosity("full")    # back to the chatty default
}
}
