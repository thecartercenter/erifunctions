% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmr.R
\name{eri_cmr_last_plan}
\alias{eri_cmr_last_plan}
\title{Reconstruct a past \code{eri_split_cmr()} run's routing plan}
\usage{
eri_cmr_last_plan(country, period, data_con = NULL)
}
\arguments{
\item{country}{\code{str} Country code (e.g. \code{"sdn"}).}

\item{period}{\code{str} Reporting period matching the run you want (e.g. \code{"202605"}).}

\item{data_con}{Azure container for the \verb{data/} blob. If \code{NULL}, connects automatically.}
}
\value{
A tibble with one row per routed sheet: \code{sheet}, \code{disease}, \code{data_type},
\code{dest}, \code{n_rows} -- identical in shape to what \code{\link[=eri_split_cmr]{eri_split_cmr()}} returns.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Recovers the routing plan (\code{sheet}, \code{disease}, \code{data_type}, \code{dest}, \code{n_rows})
for a country/period from the persisted operation log, without rerunning
\code{\link[=eri_split_cmr]{eri_split_cmr()}} or needing to have kept its return value in your R
session. \code{\link[=eri_split_cmr]{eri_split_cmr()}} records the full plan in its op-log on every
successful run; this reads the most recent one back.
}
\examples{
\dontrun{
plan <- eri_cmr_last_plan("sdn", "202605")
}
}
