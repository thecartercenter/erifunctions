% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/research.R
\name{eri_research_status}
\alias{eri_research_status}
\title{Report the data state of a research project}
\usage{
eri_research_status(path = getwd(), check_remote = FALSE, data_con = NULL)
}
\arguments{
\item{path}{\code{chr} Local project root (must contain \code{research.yaml}). Defaults to \code{getwd()}.}

\item{check_remote}{\code{lgl} If \code{TRUE}, compare each pulled input against its Azure source and flag
newer upstream versions (best-effort; needs a connection). Default \code{FALSE}.}

\item{data_con}{Azure container for the \verb{data/} blob; used only when \code{check_remote}. If \code{NULL}, connects automatically.}
}
\value{
A tibble of tracked inputs (invisibly).
}
\description{
Summarises every input the project depends on -- pulls (with update counts and whether a prior
version was archived) and artifacts -- plus the output/snapshot/tag counts, from \code{research.yaml}.
One place to answer "what does this study depend on, and is any of it stale?". With
\code{check_remote = TRUE}, flags inputs whose Azure source is newer than the local copy.
}
\examples{
\dontrun{
eri_research_status()
eri_research_status(check_remote = TRUE)
}
}
