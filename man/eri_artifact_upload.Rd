% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/artifacts.R
\name{eri_artifact_upload}
\alias{eri_artifact_upload}
\title{Upload a non-standard reference file to the artifact registry}
\usage{
eri_artifact_upload(
  local_path,
  name,
  type,
  description,
  version = NULL,
  data_con = NULL
)
}
\arguments{
\item{local_path}{\code{chr} Path to the local file to upload.}

\item{name}{\code{chr} Short identifier for this artifact (e.g. \code{"dr_irs_2024"}). Must be
unique in the registry; re-uploading the same name updates the entry (upsert).}

\item{type}{\code{chr} Artifact type. One of \code{"spatial"}, \code{"population"}, \code{"study_data"},
\code{"reference"}, \code{"other"}.}

\item{description}{\code{chr} Human-readable description of what this file contains.}

\item{version}{\code{chr} or \code{NULL} Optional version string (e.g. \code{"1.0"}, \code{"2024-05"}).}

\item{data_con}{Azure container object for the \verb{data/} blob. If \code{NULL}, connects automatically.}
}
\value{
The registered entry (invisibly).
}
\description{
Uploads a local file to \verb{artifacts/\{type\}/\{name\}/} in the \verb{data/} Azure blob and
registers it in \verb{artifacts/_registry.yaml}. Use this for files that don't go through
the standard DQ pipeline — external study data, population grids, project-specific inputs.
}
\details{
Standard spatial files already in \verb{data/spatial/} do not need to go through this function;
pull them directly via \code{\link[=eri_research_pull]{eri_research_pull()}}.
}
\examples{
\dontrun{
eri_artifact_upload(
  local_path  = "data/raw/dr_irs_campaign_2024.xlsx",
  name        = "dr_irs_2024",
  type        = "study_data",
  description = "IRS campaign data from MoH for Dominican Republic 2024"
)
}
}
