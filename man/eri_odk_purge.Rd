% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/odk_registry.R
\name{eri_odk_purge}
\alias{eri_odk_purge}
\title{Permanently remove an ODK form from the shared Azure registry}
\usage{
eri_odk_purge(project_id, form_id, server_url = NULL, data_con = NULL)
}
\arguments{
\item{project_id}{\code{int} ODK Central project ID.}

\item{form_id}{\code{str} ODK Central form ID (xmlFormId).}

\item{server_url}{\code{str} or \code{NULL} ODK Central server URL. If \code{NULL}, matches on
\code{project_id} and \code{form_id} alone (removes every server's matching entry).}

\item{data_con}{Azure container object for the \verb{data/} blob. If \code{NULL}, connects automatically.}
}
\value{
Invisibly, the number of entries removed.
}
\description{
\strong{Hard-deletes} every matching registry entry — active \emph{or} already
soft-deleted — removing it from \code{odk/registry.yaml} entirely. Unlike
\code{\link[=eri_odk_deregister]{eri_odk_deregister()}}, which soft-deletes (\code{active: false}) and preserves the
sync history, this leaves no trace. Use it to clean up \strong{practice / sandbox}
registrations (which otherwise linger as inactive rows in the shared registry);
for a real form prefer \code{eri_odk_deregister()}, which keeps the audit trail.
}
\examples{
\dontrun{
# Tear down a sandbox registration completely
eri_odk_purge(project_id = 99999, form_id = "eri_test_river_prospection")
}
}
