% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dal.R
\name{eri_trigger}
\alias{eri_trigger}
\title{Trigger a registered GitHub Actions pipeline}
\usage{
eri_trigger(
  pipeline,
  country,
  disease,
  year = NULL,
  phase = "prod",
  ref = "main"
)
}
\arguments{
\item{pipeline}{\code{str} Registered pipeline name. Currently \code{"hsp-mal"}.}

\item{country}{\code{str} Country code passed as a workflow input (e.g. \code{"dr"}).}

\item{disease}{\code{str} Disease name passed as a workflow input (e.g. \code{"malaria"}).}

\item{year}{\code{int} or \code{NULL} Optional year passed as a workflow input. Default \code{NULL}.}

\item{phase}{\code{str} Pipeline phase. Default \code{"prod"}; use \code{"testing"} for dry runs.}

\item{ref}{\code{str} Branch or tag to run the workflow against. Default \code{"main"}.}
}
\value{
Invisibly, the URL to the workflow's runs page on GitHub.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Dispatches a \code{workflow_dispatch} event to a registered GitHub Actions
pipeline from R.  Authenticates with a GitHub Personal Access Token stored
in the \code{GITHUB_PAT} environment variable (needs \code{workflow} scope).
\subsection{Registered pipelines}{\tabular{lll}{
   Name \tab Repository \tab Workflow \cr
   \code{hsp-mal} \tab thecartercenter/health-hsp-malaria \tab data_ingestion.yml \cr
}

}
}
\examples{
\dontrun{
eri_trigger("hsp-mal", "dr", "malaria", phase = "testing")
}
}
