% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/onboarding.R
\name{eri_onboard_country}
\alias{eri_onboard_country}
\title{Scaffold a new country/disease surveillance setup}
\usage{
eri_onboard_country(
  country_code,
  country_name,
  disease,
  language = "en",
  path = getwd(),
  data_con = NULL,
  dry_run = FALSE
)
}
\arguments{
\item{country_code}{\code{chr} Short country code (e.g. \code{"uga"}, \code{"eth"}).}

\item{country_name}{\code{chr} Full country name as it appears in data (e.g. \code{"Uganda"}).}

\item{disease}{\code{chr} Disease code (e.g. \code{"oncho"}, \code{"malaria"}, \code{"lf"}).}

\item{language}{\code{chr} Language for schema comments (\code{"en"} or \code{"fr"}). Default \code{"en"}.}

\item{path}{\code{chr} Directory to write the schema YAML into. Default is the current
working directory.}

\item{data_con}{Azure container for the \verb{data/} blob. If \code{NULL}, connects automatically.
Ignored when \code{dry_run = TRUE}.}

\item{dry_run}{\code{lgl} If \code{TRUE}, print a plan but do not write files or create Azure
directories. Default \code{FALSE}.}
}
\value{
Invisibly, the path to the written schema file (or \code{NULL} in dry-run mode).
}
\description{
Writes a DQ schema YAML template to your local working directory and
creates the three-layer Azure blob directories for the new country/disease.
Edit the YAML locally, then submit it to the package via a pull request
when it is ready for team-wide use.
}
\examples{
\dontrun{
eri_onboard_country("uga", "Uganda", "oncho")
eri_onboard_country("nga", "Nigeria", "lf", language = "en", dry_run = TRUE)
}
}
