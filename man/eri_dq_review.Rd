% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dq_review.R
\name{eri_dq_review}
\alias{eri_dq_review}
\title{Interactively review and resolve a CMR workbook's DQ flags, then approve}
\usage{
eri_dq_review(country, period, plan = NULL, data_con = NULL)
}
\arguments{
\item{country}{\code{str} Country code (e.g. \code{"sdn"}).}

\item{period}{\code{str} Reporting period (e.g. \code{"202605"}).}

\item{plan}{\code{tibble} or \code{NULL} The plan from \code{\link[=eri_split_cmr]{eri_split_cmr()}} / \code{\link[=eri_cmr_last_plan]{eri_cmr_last_plan()}}. \code{NULL}
(default) looks it up via \code{\link[=eri_cmr_last_plan]{eri_cmr_last_plan()}}.}

\item{data_con}{Azure container for the \verb{data/} blob. If \code{NULL}, connects automatically.}
}
\value{
Invisibly, one of \code{"approved"}, \code{"force_approved"}, or \code{"exited"} -- how the session
ended, for a caller like \code{\link[=eri_do]{eri_do()}} that hands off into this same loop and needs to know
whether to print its own closing message. Every effect happens through the scriptable core it
calls, which is where the real return values (approvals, resolved flags, submitted tickets)
live.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

The interactive front door over the scriptable DQ core built in Phases 2-6 of the DQ workflow
redesign: \code{\link[=eri_cmr_dq_report]{eri_cmr_dq_report()}} for the combined flags tibble, \code{\link[=eri_dq_flag_resolve]{eri_dq_flag_resolve()}} and
\code{\link[=eri_logs_resolve]{eri_logs_resolve()}} for triage, \code{\link[=eri_dq_schema_edit]{eri_dq_schema_edit()}}/\code{\link[=eri_dq_schema_submit]{eri_dq_schema_submit()}} for schema
fixes, \code{\link[=eri_split_cmr]{eri_split_cmr()}} for re-running against a corrected workbook, and \code{\link[=eri_approve_cmr]{eri_approve_cmr()}}
(including its \code{force = TRUE} path) for the sign-off. Every mutation goes through one of those
functions immediately -- this wrapper holds no state of its own beyond one in-memory,
per-call path cache (which local workbook you're fixing this session), so closing the laptop
mid-review and running \code{eri_dq_review()} again later picks up exactly where the log YAMLs say
things are.

The loop: clean -> offered approval; flagged -> work through flags one at a time (fix in the
source workbook, adjust the schema, or mark not-important/noted), re-run, force-approve, print
a report, or exit. CMR-only for now -- the plan machinery this dispatches to
(\code{\link[=eri_cmr_last_plan]{eri_cmr_last_plan()}}, \code{\link[=eri_split_cmr]{eri_split_cmr()}}) is CMR-specific; generalizing to other ingest shapes
later only touches this orchestration layer.

\strong{Interactive only.} In a script or CI, use the scriptable core directly:
\code{\link[=eri_cmr_dq_report]{eri_cmr_dq_report()}}, \code{\link[=eri_dq_flag_resolve]{eri_dq_flag_resolve()}}, \code{\link[=eri_logs_resolve]{eri_logs_resolve()}}, \code{\link[=eri_approve_cmr]{eri_approve_cmr()}}.
}
\examples{
\dontrun{
eri_dq_review("sdn", "202605")
}
}
\seealso{
\code{\link[=eri_cmr_dq_report]{eri_cmr_dq_report()}}, \code{\link[=eri_dq_flag_resolve]{eri_dq_flag_resolve()}}, \code{\link[=eri_logs_resolve]{eri_logs_resolve()}},
\code{\link[=eri_approve_cmr]{eri_approve_cmr()}} for the scriptable core this orchestrates.
}
