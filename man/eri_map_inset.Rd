% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maps.R
\name{eri_map_inset}
\alias{eri_map_inset}
\title{Add an inset reference map to a main map}
\usage{
eri_map_inset(
  main_map,
  country_sf,
  highlight_sf,
  position = c(0.65, 0.05, 0.32, 0.38),
  highlight_color = "#e63946"
)
}
\arguments{
\item{main_map}{A ggplot object (the primary map).}

\item{country_sf}{An \code{sf} object for the country/region outline.}

\item{highlight_sf}{An \code{sf} object for the study area to highlight in red.}

\item{position}{\code{num} vector of length 4 giving the inset position and size
as fractions of the main map: \code{c(xmin, ymin, width, height)}. Default
\code{c(0.65, 0.05, 0.32, 0.38)}.}

\item{highlight_color}{\code{chr} Fill colour for the highlighted area.
Default \code{"#e63946"} (red).}
}
\value{
A cowplot \code{ggdraw} object (also a ggplot).
}
\description{
Draws a small reference map showing \code{country_sf} in grey with \code{highlight_sf}
shaded red, then overlays it on \code{main_map} at the requested position using
\code{\link[cowplot:ggdraw]{cowplot::ggdraw()}}. The inset uses \code{eri_plot_theme("map.inset")} (void
theme with a black border).
}
\details{
Requires the \code{cowplot} package.
}
\examples{
\dontrun{
haiti <- eri_spatial_load("ht", level = 0)
dept  <- eri_spatial_load("ht", level = 1)
main  <- eri_map_choropleth(dept, data, "n_cases", "adm1_name")
eri_map_inset(main, haiti, dept)
}
}
