% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feedback.R
\name{eri_feedback_list}
\alias{eri_feedback_list}
\title{List logged feedback}
\usage{
eri_feedback_list(area = NULL, status = NULL, data_con = NULL)
}
\arguments{
\item{area}{\code{chr} or \code{NULL} Filter to one section (e.g. \code{"odk"}). \code{NULL} = all.}

\item{status}{\code{chr} or \code{NULL} Filter by status (e.g. \code{"submitted"}). \code{NULL} = all.}

\item{data_con}{Azure container object for the \verb{data/} blob. If \code{NULL}, connects automatically.}
}
\value{
A tibble with columns \code{id}, \code{submitted_at}, \code{submitted_by}, \code{area},
\code{status}, \code{message}, \code{context} (a list-column: \code{NULL} or a named list per
ticket), \code{attachment} (blob path, or \code{NA} if none).
}
\description{
Reads the team's feedback backlog from \verb{_feedback/feedback_log.yaml} in the
\verb{data/} Azure blob into a tibble, in the order tickets were filed. Optional
filters narrow by \code{area} or \code{status}.
}
\examples{
\dontrun{
eri_feedback_list()
eri_feedback_list(area = "odk")
eri_feedback_list(status = "submitted")
}
}
\seealso{
\code{\link[=eri_feedback]{eri_feedback()}} to file a ticket.
}
