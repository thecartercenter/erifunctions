% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{eri_spatial_pop}
\alias{eri_spatial_pop}
\title{Extract population from LandScan into spatial polygons}
\usage{
eri_spatial_pop(shapefile, year = NULL, data_con = NULL, fun = "sum")
}
\arguments{
\item{shapefile}{An \code{sf} polygon object (e.g. from \code{\link[=eri_spatial_load]{eri_spatial_load()}}).}

\item{year}{\code{int} LandScan year to use. If \code{NULL} (default), uses the latest
year available in Azure.}

\item{data_con}{Azure container object for the \verb{data/} blob. If \code{NULL}, connects automatically.}

\item{fun}{\code{chr} Summary function passed to \code{exactextractr::exact_extract()}.
Default \code{"sum"} returns total population per polygon.}
}
\value{
The input \code{shapefile} with a \code{pop} column added (numeric).
}
\description{
Downloads a LandScan raster from Azure, extracts population counts for each
feature in \code{shapefile} using \code{exactextractr::exact_extract()}, and returns
the \code{sf} object with a \code{pop} column added.
}
\details{
By default the most recent LandScan year available in Azure is used. Older
years are accessible via \code{year}. Run \code{\link[=eri_landscan_list]{eri_landscan_list()}} to see what is
available.
}
\examples{
\dontrun{
communes <- eri_spatial_load("ht", level = 2) |>
  eri_spatial_pop()

# Use a specific year
communes_2022 <- eri_spatial_load("ht", level = 2) |>
  eri_spatial_pop(year = 2022)
}
}
