% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dal.R
\name{.eri_create_azure_dir}
\alias{.eri_create_azure_dir}
\title{Ensure an Azure directory exists, creating any missing parents.}
\usage{
.eri_create_azure_dir(azcontainer, path)
}
\arguments{
\item{azcontainer}{Azure container object.}

\item{path}{\code{chr} Directory path to ensure exists.}
}
\value{
The trimmed path (invisibly).
}
\description{
ADLS Gen2 rejects a trailing slash in directory operations (HTTP 400, "the request URI is
invalid") and does not reliably create intermediate parents, so we strip trailing slashes and
create each level of the path that is missing. On flat blob storage these are cheap no-ops.
This is the canonical directory-creation primitive: \code{\link[=azure_io]{azure_io()}}'s \code{"create"} op and every
nested-path write site (\code{research.R}, \code{artifacts.R}, \code{catalog.R}, \code{odk_registry.R},
\code{onboarding.R}, \code{cmr.R}, \code{templates.R}) route through it rather than calling
\code{AzureStor::create_storage_dir()} directly.
}
\keyword{internal}
