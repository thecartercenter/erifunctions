% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/research.R
\name{eri_research_snapshot}
\alias{eri_research_snapshot}
\title{Snapshot the full research project data directory to Azure}
\usage{
eri_research_snapshot(label = NULL, path = getwd(), data_con = NULL)
}
\arguments{
\item{label}{\code{chr} or \code{NULL} Optional short label for this snapshot (e.g. \code{"pre-ITS-run"}).}

\item{path}{\code{chr} Local project root (must contain \code{research.yaml} and \verb{data/}). Defaults to \code{getwd()}.}

\item{data_con}{Azure container object for the \verb{data/} blob. If \code{NULL}, connects automatically.}
}
\value{
Azure snapshot path (invisibly).
}
\description{
Uploads every file in the local \verb{data/} directory to
\verb{research/\{project_name\}/snapshots/\{timestamp\}/} in the \verb{data/} Azure blob,
writes a \verb{_manifest.yaml} alongside listing what was included, and records
the snapshot in \code{research.yaml}.
}
\details{
Use this to freeze a reproducible checkpoint of all input data before a major
analysis run or before sharing results.
}
\examples{
\dontrun{
eri_research_snapshot(label = "pre-ITS-run")
}
}
