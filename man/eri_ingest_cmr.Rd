% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmr.R
\name{eri_ingest_cmr}
\alias{eri_ingest_cmr}
\title{Read and parse a CMR monthly report Excel file}
\usage{
eri_ingest_cmr(path, sheet, country = NULL)
}
\arguments{
\item{path}{\code{str} Local path to the CMR Excel file.}

\item{sheet}{\code{str} or \code{int} Sheet name, 1-based index, or canonical slug
(e.g. \code{"rb_treatment"}). Slugs are resolved to actual sheet names via the
country schema's \code{sheet_aliases} block when \code{country} is supplied.}

\item{country}{\code{str} or \code{NULL} Optional country code (e.g. \code{"tcd"}, \code{"uga"}).
When supplied, the country code is prepended as a \code{country} column and slug
aliases are resolved. Default \code{NULL}.}
}
\value{
A tibble with field-code column names and data from row 6 onward,
plus an \code{excel_row} column recording each row's real position in the
workbook (survives all-NA spacer-row dropping, so it stays accurate even
after rows are removed). If \code{country} is supplied it is prepended as a
\code{country} column.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Reads a single sheet from a Carter Center RBLF monthly report template,
using the machine-readable field code row (row 5 of the template) as column
names. Field codes (e.g. \verb{#rbtrt_year}, \verb{#rbtrt_adm1}) are consistent across
all country templates regardless of language, so the same function parses
both English and French templates.
\subsection{Template structure assumed}{\tabular{ll}{
   Row \tab Content \cr
   1 \tab Sheet title \cr
   2 \tab Empty spacer \cr
   3 \tab Group headers (Location / Targets / Month columns) \cr
   4 \tab Human-readable column names \cr
   5 \tab Machine-readable field codes — \strong{parsing anchor} \cr
   6+ \tab Data \cr
}

}
}
\examples{
\dontrun{
# English template — sheet name directly
df <- eri_ingest_cmr("data/uga_2024_01.xlsx", sheet = "RB Treatment", country = "uga")
# French template — canonical slug resolved via schema
df <- eri_ingest_cmr("data/tcd_2024_01.xlsx", sheet = "rb_treatment", country = "tcd")
}
}
\seealso{
Other CMR pipeline functions: 
\code{\link{eri_approve_cmr}()},
\code{\link{eri_cmr_dq_report}()},
\code{\link{eri_cmr_last_plan}()},
\code{\link{eri_split_cmr}()},
\code{\link{eri_stage_cmr}()}
}
\concept{CMR pipeline functions}
