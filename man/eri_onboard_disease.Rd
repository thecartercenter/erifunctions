% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/onboarding.R
\name{eri_onboard_disease}
\alias{eri_onboard_disease}
\title{Scaffold DQ schema YAML files for a new disease program}
\usage{
eri_onboard_disease(
  disease,
  country,
  data_types = c("mda", "prevalence"),
  output_dir = getwd(),
  dry_run = FALSE
)
}
\arguments{
\item{disease}{\code{chr} Short disease code (e.g. \code{"rb"}, \code{"schisto"}, \code{"sth"}).}

\item{country}{\code{chr} Country or program code (e.g. \code{"ug"}, \code{"global"}).}

\item{data_types}{\code{chr} vector Data types to scaffold. Each generates one
file. Supported values: \code{"mda"}, \code{"prevalence"}. Default both.}

\item{output_dir}{\code{chr} Directory to write skeleton YAML files into.
Default is the current working directory.}

\item{dry_run}{\code{lgl} If \code{TRUE}, print a plan but do not write files.
Default \code{FALSE}.}
}
\value{
Invisibly, a character vector of paths written (or \code{NULL} in
dry-run mode).
}
\description{
Generates one skeleton YAML file per \code{data_type} (e.g. \code{"mda"},
\code{"prevalence"}) following the standard column layout for each type.
TODO comments in the generated files flag fields that must be customised
before the schema is ready for team-wide use.
}
\examples{
\dontrun{
eri_onboard_disease("schisto", "ug", output_dir = "schemas/")
eri_onboard_disease("rb", "ug", data_types = "mda", dry_run = TRUE)
}
}
