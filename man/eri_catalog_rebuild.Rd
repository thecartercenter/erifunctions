% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog.R
\name{eri_catalog_rebuild}
\alias{eri_catalog_rebuild}
\title{Rebuild the data catalog by scanning the processed layer}
\usage{
eri_catalog_rebuild(data_con = NULL)
}
\arguments{
\item{data_con}{Azure container object for the \verb{data/} blob. If \code{NULL}, connects automatically.}
}
\value{
The rebuilt catalog tibble (invisibly), as from \code{\link[=eri_catalog_query]{eri_catalog_query()}}.
}
\description{
Reconstructs \verb{_catalog/data_catalog.yaml} from the actual processed-layer
Parquet files in the \verb{data/} Azure blob, making the catalog a \strong{derivable
cache} rather than an irreplaceable record (ADR-0002). Every
\verb{*/processed/*.parquet} path matching the five-axis data model
(\verb{\{country\}/\{disease\}/\{data_source\}/\{data_type\}/processed/}) — or the legacy
four-axis form — becomes an entry. \code{registered_by} is set to \code{"rebuilt"} and
\code{row_count} is left \code{NA} (the file is not opened). Use it to recover from a
lost or corrupted catalog, or to pick up files written outside
\code{\link[=eri_catalog_register]{eri_catalog_register()}}.
}
\details{
The rebuilt catalog \strong{replaces} the existing one; entries for files that no
longer exist are dropped. Provenance fields that can only come from the
original registration (the real \code{registered_by}, \code{row_count}) are not
recovered — re-run \code{\link[=eri_catalog_verify]{eri_catalog_verify()}} afterwards if needed.
}
\examples{
\dontrun{
eri_catalog_rebuild()
}
}
