% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi_lf.R
\name{eri_lf_tas_summary}
\alias{eri_lf_tas_summary}
\title{Summarise LF TAS antigen test results}
\usage{
eri_lf_tas_summary(data, fts_col, rdt_col, group_col = NULL)
}
\arguments{
\item{data}{A data frame of individual TAS results (one row per person).}

\item{fts_col}{\code{chr} Column with FTS results (e.g. \code{"Positive"} / \code{"Negative"}).}

\item{rdt_col}{\code{chr} Column with RDT results (e.g. \code{"Positive"} / \code{"Negative"} / \code{NA}).}

\item{group_col}{\code{chr} or \code{NULL}. If supplied, the summary is produced per
unique value of this column (e.g. \code{"commune"} or \code{"eu"}).}
}
\value{
A tibble with columns \code{fts_result}, \code{rdt_result}, \code{n}, \code{pct}
(and \code{group_col} if supplied).
}
\description{
Cross-tabulates FTS and RDT results from an individual-level TAS dataset and
returns a tidy tibble with one row per FTS/RDT combination. Optionally
grouped by a geographic or survey unit column.
}
\examples{
\dontrun{
eri_lf_tas_summary(tas_data, "fts_result", "rdt_result", group_col = "commune")
}
}
