% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/logs.R
\name{eri_dq_flag_resolve}
\alias{eri_dq_flag_resolve}
\title{Triage a single DQ flag within a logged \code{eri_dq_log()} entry}
\usage{
eri_dq_flag_resolve(
  flag_id,
  status = c("not_important", "fixed", "noted"),
  note = NULL,
  data_con = NULL
)
}
\arguments{
\item{flag_id}{\code{chr} A flag identifier from \code{\link[=eri_cmr_dq_report]{eri_cmr_dq_report()}} (or built
by hand as \code{paste0(log_path, "::", index)}, where \code{index} is the flag's
1-based position within that log entry).}

\item{status}{\code{chr} One of \code{"not_important"}, \code{"fixed"}, or \code{"noted"}.}

\item{note}{\code{chr} or \code{NULL} What you did or decided for this specific flag.}

\item{data_con}{Azure container for the \verb{data/} blob. If \code{NULL}, connects automatically.}
}
\value{
Invisibly, \code{TRUE}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Works through \strong{one flag at a time} rather than an entire DQ-log entry:
marks a specific flag \code{"not_important"}, \code{"fixed"}, or \code{"noted"}, with an
optional note, so a DA can triage a multi-flag entry (e.g. one CMR measure
with several issues) issue by issue instead of all-or-nothing. Distinct
from \code{\link[=eri_logs_resolve]{eri_logs_resolve()}}, which closes out the \emph{whole} entry (and marks
it \code{handled}, dropping it from the open backlog / unblocking
\code{\link[=eri_approve_cmr]{eri_approve_cmr()}}) -- resolving every individual flag here does not by
itself mark the entry handled; call \code{\link[=eri_logs_resolve]{eri_logs_resolve()}} afterward for
that (it will auto-summarize from the per-flag decisions if you don't pass
your own note).
}
\examples{
\dontrun{
flags <- eri_cmr_dq_report("sdn", "202605")
eri_dq_flag_resolve(flags$flag_id[1], "fixed", note = "corrected district spelling upstream")
eri_dq_flag_resolve(flags$flag_id[2], "not_important", note = "known template quirk")
}
}
