% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{eri_inject_anomalies}
\alias{eri_inject_anomalies}
\title{Inject controllable anomalies into a clean dataset}
\usage{
eri_inject_anomalies(
  data,
  types = .ERI_ANOMALY_TYPES,
  n = 1L,
  cols = NULL,
  seed = NULL
)
}
\arguments{
\item{data}{A data frame to perturb.}

\item{types}{\code{chr} Which anomalies to inject. Any of
missing, outlier, negative, typo, duplicate, drop. Defaults to all.}

\item{n}{\code{int} How many anomalies to inject \strong{per type} (cells for
cell-level types, rows for \code{duplicate}/\code{drop}). Capped at what's available.
Default \code{1}.}

\item{cols}{\code{chr} or \code{NULL} Restrict the cell-level types (\code{missing},
\code{outlier}, \code{negative}, \code{typo}) to these columns. \code{NULL} (default) auto-picks
eligible columns per type (numeric for \code{outlier}/\code{negative},
character/factor for \code{typo}, any for \code{missing}).}

\item{seed}{\code{int} or \code{NULL} Optional RNG seed for a reproducible perturbation
(set locally; the global RNG state is left untouched).}
}
\value{
\code{data} with anomalies injected, plus an \code{"eri_anomalies"} attribute
(a tibble of what was changed).
}
\description{
Perturbs a data frame with a chosen set of realistic, reproducible anomalies —
the simulation-harness counterpart to the \verb{add_anomaly_*} detectors. Use it to
stand in dirty "new data" for otherwise-clean staged files so the data-quality
and reconciliation paths (\code{run_dq_checks()}, \code{\link[=eri_compare]{eri_compare()}}) are genuinely
exercised (roadmap Phase 3).
}
\details{
Anomaly types:
\itemize{
\item \code{missing} — set cells to \code{NA}.
\item \code{outlier} — replace numeric cells with an extreme value.
\item \code{negative} — make numeric cells implausibly negative (e.g. negative counts).
\item \code{typo} — perturb character/factor cells (case, stray characters, whitespace).
\item \code{duplicate} — duplicate whole rows.
\item \code{drop} — remove whole rows.
}

The result carries an \code{"eri_anomalies"} attribute: a tibble logging every
injection (\code{type}, \code{row}, \code{column}, \code{original}, \code{new}) — the ground truth a
simulation can check detection against. \code{row} is the row index in the input
\code{data}.
}
\examples{
clean <- data.frame(
  id = 1:10, cases = c(5, 8, 3, 6, 9, 4, 7, 2, 5, 8), site = letters[1:10]
)
dirty <- eri_inject_anomalies(clean, types = c("missing", "outlier"), n = 2, seed = 1)
attr(dirty, "eri_anomalies")
}
\seealso{
\code{\link[=eri_compare]{eri_compare()}} to reconcile, \code{run_dq_checks()} to detect.
}
