% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/research.R
\name{eri_research_resume}
\alias{eri_research_resume}
\title{Resume a research project session}
\usage{
eri_research_resume(path = getwd(), data_con = NULL)
}
\arguments{
\item{path}{\code{chr} Local project root (must contain \code{research.yaml}). Defaults to \code{getwd()}.}

\item{data_con}{Azure container object for the \verb{data/} blob. If \code{NULL}, connects automatically.}
}
\value{
The manifest list (invisibly).
}
\description{
Reads \code{research.yaml} from the project root, re-establishes the Azure connection,
and prints a session summary (last pull, last log entry, snapshot count). Call this
at the top of each work session instead of re-typing project context.
}
\examples{
\dontrun{
eri_research_resume()
}
}
