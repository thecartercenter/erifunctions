% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dal.R
\name{azure_io}
\alias{azure_io}
\title{Helper function to read and write key data to the Azure environment}
\usage{
azure_io(
  io,
  file_loc = NULL,
  obj = NULL,
  azcontainer = suppressMessages(get_azure_storage_connection()),
  force_delete = FALSE,
  local_path = NULL,
  progress = FALSE,
  ...
)
}
\arguments{
\item{io}{\code{str} The type of operation to perform in Azure
\itemize{
\item \code{"read"} Read a file from Azure, must be an rds, csv, rda, or xls/xlsx file.
\item \code{"write"} Write a file to Azure, must be an rds, csv, rda, or xls/xlsx file. To
write an Excel file with multiple sheets, pass a named list containing the tibbles
of interest. See examples.
\item \code{"exists.dir"} Returns a boolean after checking to see if a folder exists.
\item \code{"exists.file"}Returns a boolean after checking to see if a file exists.
\item \code{"create"} Creates a folder and all preceding folders.
\item \code{"list"} Returns a tibble with all objects in a folder.
\item \code{"upload"} Moves a file of any type to Azure
\item \code{"delete"} Deletes a file.
\item \code{"delete.dir"} Deletes a folder.
}}

\item{file_loc}{\code{str} Location to "read", "write", "exists.dir", "exists.file", "create" or "list".}

\item{obj}{\code{robj} Object to be saved, needed for \code{"write"}. Defaults to \code{NULL}.}

\item{azcontainer}{Azure container object returned from \code{\link[=get_azure_storage_connection]{get_azure_storage_connection()}}.}

\item{force_delete}{\code{logical} Use delete io without confirmation prompt. Default \code{FALSE}.}

\item{local_path}{\code{str} Local file pathway to upload a file to Azure. Default is \code{NULL}.
This parameter is only required when passing \code{"upload"} in the \code{io} parameter.}

\item{progress}{\code{logical} Show AzureStor's byte progress bar for the transfer. Default \code{FALSE}
(suppressed). Set \code{TRUE} for a large single read/upload that needs visible feedback.}

\item{...}{Optional parameters that work with \code{\link[readr:read_delim]{readr::read_delim()}}, \code{\link[readxl:read_excel]{readxl::read_excel()}}, or \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
Output dependent on argument passed in the \code{io} parameter.
}
\description{
The function serves as the primary way to interact with the Azure system from R. It can
read, write, create folders, check whether a file or a folder exists, upload files, and list
all files in a folder.
}
\examples{
\dontrun{
df <- azure_io("read", file_loc = "df1.csv")
df2 <- azure_io("read", file_loc = "df2.xlsx", sheet = 1, skip = 2)
list_of_df <- list(df_1 = df, df_2 = df)
azure_io("write", file_loc = "Data/test/df.csv", obj = df)
azure_io("write", file_loc = "Data/test/df.xlsx", obj = list_of_df)
azure_io("exists.dir", "Data/nonexistentfolder")
azure_io("exists.file", file_loc = "Data/test/df1.csv")
azure_io("create", "Data/nonexistentfolder")
azure_io("list")
azure_io("upload", file_loc = "Data/test", local_path = "C:/Users/ABC1/Desktop/df2.csv")
}
}
