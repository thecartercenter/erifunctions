% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{eri_query}
\alias{eri_query}
\title{Query processed data with SQL (serverless DuckDB)}
\usage{
eri_query(
  sql,
  country = NULL,
  disease = NULL,
  data_source = NULL,
  data_type = NULL,
  period = NULL,
  table = "data",
  tables = NULL,
  data_con = NULL
)
}
\arguments{
\item{sql}{\code{chr} A single SQL statement (DuckDB dialect) referencing the
in-scope table names.}

\item{country, disease, data_source, data_type, period}{Catalog filters selecting
the \strong{processed} datasets to attach as the \code{table} view. Any combination;
all \code{NULL} (default) means "use only \code{tables}".}

\item{table}{\code{chr} Name for the catalog-driven table in SQL (default \code{"data"}).}

\item{tables}{Named list of additional tables to register: each entry a
data.frame, a local parquet path, or a \verb{data/} blob path.}

\item{data_con}{Azure container for the \verb{data/} blob; \code{NULL} connects
automatically (only when a catalog query or a blob path is actually needed,
so pure data.frame queries stay offline).}
}
\value{
A tibble of the query result.
}
\description{
Run SQL across one or more \strong{processed} datasets without standing up a database
or writing a chain of per-file reads. \code{eri_query()} attaches the relevant
parquet into an in-process \href{https://duckdb.org}{DuckDB} session and returns the
result as a tibble. The Azure blob stays the system of record (ADR-0004).
}
\details{
There are two ways to put data in scope, and they compose:
\itemize{
\item \strong{Catalog-driven (roll-ups).} Pass any of \code{country} / \code{disease} /
\code{data_source} / \code{data_type} / \code{period}; \code{eri_query()} looks up the matching
\strong{processed} files in the data catalog, reads them, stamps each row with its
\code{country} / \code{disease} / \code{data_source} / \code{data_type} / \code{period}, and unions them
into a single table named by \code{table} (default \code{"data"}). This makes
cross-country / cross-period aggregation a one-liner:
\verb{SELECT country, SUM(total_cases) FROM data GROUP BY country}.
\item \strong{Explicit tables (joins).} Pass \code{tables = list(name = x)} where each \code{x} is a
data.frame, a local \code{.parquet} path, or a \verb{data/} blob path. Each is registered
under its name so you can join, e.g. cases to a population table.
}
}
\section{Requirements}{

Needs the optional \code{duckdb} and \code{DBI} packages
(\code{install.packages(c("duckdb", "DBI"))}). Very large cross-dataset scans are
bounded by local memory / download (ADR-0004).
}

\examples{
\dontrun{
# Cross-country roll-up from the catalog
eri_query(
  "SELECT country, SUM(total_cases) AS cases FROM data GROUP BY country ORDER BY cases DESC",
  disease = "malaria", data_type = "aggregate"
)

# Join an approved dataset to a population table you supply
eri_query(
  "SELECT d.adm1, SUM(d.cases) * 1000.0 / p.pop AS rate
     FROM data d JOIN pop p USING (adm1) GROUP BY d.adm1, p.pop",
  disease = "malaria", data_type = "case", tables = list(pop = pop_df)
)
}
}
