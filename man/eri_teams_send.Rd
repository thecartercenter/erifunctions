% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/teams.R
\name{eri_teams_send}
\alias{eri_teams_send}
\title{Send a message to Microsoft Teams}
\usage{
eri_teams_send(message, to = NULL, team = NULL, channel = NULL, token = NULL)
}
\arguments{
\item{message}{A single character string. The message body.}

\item{to}{Email or user ID of the recipient for a 1:1 DM. Pass \code{"self"} or
\code{NULL} to message yourself (Graph API only).}

\item{team}{Display name of the Teams team (for channel messages, Graph API).}

\item{channel}{Display name of the channel within \code{team} (Graph API), or
ignored when using a webhook (the webhook URL already encodes the channel).}

\item{token}{A Graph API bearer token. If \code{NULL}, checks
\code{ERIFUNCTIONS_TEAMS_TOKEN}; falls back to the incoming webhook if
\code{ERIFUNCTIONS_TEAMS_WEBHOOK} is set.}
}
\value{
Invisibly returns the \code{httr} response object.
}
\description{
Sends a plain-text message to a Teams channel or individual. Supports two
delivery paths: an \strong{incoming webhook} (no auth required, channel only) and
the \strong{Graph API} (requires a bearer token, supports DMs and channels).
}
\details{
\strong{Delivery routing:}
\enumerate{
\item \code{team} + \code{channel} supplied, token available \eqn{\rightarrow} Graph API channel message.
\item \code{to} supplied (or omitted), token available \eqn{\rightarrow} Graph API DM.
\item \code{ERIFUNCTIONS_TEAMS_WEBHOOK} set, no token \eqn{\rightarrow} incoming webhook.
\item Otherwise an error is raised with setup instructions.
}
}
