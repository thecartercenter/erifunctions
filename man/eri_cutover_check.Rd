% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cutover.R
\name{eri_cutover_check}
\alias{eri_cutover_check}
\title{Compare a stream's period and record it in the cutover ledger}
\usage{
eri_cutover_check(
  new,
  old,
  country,
  disease,
  data_source,
  period,
  by,
  data_type = NULL,
  tolerance = 0,
  ignore = NULL,
  record = TRUE,
  data_con = NULL,
  new_con = NULL,
  old_con = NULL
)
}
\arguments{
\item{new, old}{The new (\code{data/staged}) and reference (legacy \code{projects/intermediate})
datasets — data frames or Azure blob paths, as in \code{\link[=eri_compare]{eri_compare()}}.}

\item{country, disease, data_source}{\code{chr} The stream's axes.}

\item{period}{\code{chr} The period being checked (e.g. \code{"2024_06"}, \code{"2024-W01"}).}

\item{by}{\code{chr} Key column(s) uniquely identifying a row (required — the gate
needs per-cell reconciliation).}

\item{data_type}{\code{chr} or \code{NULL} The measure, where it splits the stream.}

\item{tolerance}{\code{num} Absolute numeric tolerance for the comparison. Default \code{0}.}

\item{ignore}{\code{chr} or \code{NULL} Columns to exclude from the comparison.}

\item{record}{\code{lgl} Append the outcome to the ledger? Default \code{TRUE}.}

\item{data_con}{Azure container for the \verb{data/} blob (the ledger). If \code{NULL}, connects automatically.}

\item{new_con, old_con}{Passed to \code{\link[=eri_compare]{eri_compare()}} when \code{new}/\code{old} are blob paths.}
}
\value{
The \code{\link[=eri_compare]{eri_compare()}} result (an \code{eri_comparison}), invisibly.
}
\description{
Runs the \strong{cutover-standard} comparison — \code{eri_compare(new, old, by, strict_schema = FALSE, tolerance, ignore)} — for one data stream's \code{period} and
appends the outcome to \verb{_cutover/cutover_log.yaml} in the \verb{data/} blob. This is
the per-period evidence the cutover gate is built on (ADR-0015): run it each
period of the parallel run, then check the streak with \code{\link[=eri_cutover_status]{eri_cutover_status()}}.
}
\details{
\code{strict_schema = FALSE} is fixed (not exposed): the cutover gate requires
value/row parity but tolerates extra columns the new pipeline adds. The \code{by}
keys and \code{tolerance} are recorded with the entry so the bar is auditable.

To \strong{accept} a legitimately-expected difference (ADR-0015), record the period
with that difference excluded — pass the differing column to \code{ignore}, or widen
\code{tolerance} — so the period reconciles under the relaxed standard, which is
itself recorded in the ledger entry (visible and attributable, not hidden).
}
\examples{
\dontrun{
eri_cutover_check(
  new = "uga/oncho/programmatic/treatment/staged/2024_06.parquet",
  old = "health-rb-country-expansion-dev/intermediate/uga/2024_06.parquet",
  country = "uga", disease = "oncho", data_source = "programmatic",
  data_type = "treatment", period = "2024_06", by = c("admin2", "period")
)
}
}
\seealso{
\code{\link[=eri_cutover_status]{eri_cutover_status()}} for the streak, \code{\link[=eri_compare]{eri_compare()}} for the engine.
}
