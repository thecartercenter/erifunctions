% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/odk_survey_status.R
\name{eri_survey_status}
\alias{eri_survey_status}
\title{ODK form submission metrics}
\usage{
eri_survey_status(project_id = NULL, form_id = NULL, con = NULL)
}
\arguments{
\item{project_id}{\code{int} ODK project ID, or \code{NULL} for all projects.}

\item{form_id}{\code{chr} ODK form ID, or \code{NULL} for all forms in the project.}

\item{con}{An \code{odk_connection} object from \code{\link[=init_odk_connection]{init_odk_connection()}}, or \code{NULL}
to fall back to the \code{ODK_URL} and \code{ODK_TOKEN} environment variables.}
}
\value{
An S3 object of class \code{c("survey_status", "tbl_df", "tbl", "data.frame")}
with the following columns:
\describe{
\item{project_id}{\code{int} ODK project ID.}
\item{project_name}{\code{chr} ODK project display name.}
\item{form_id}{\code{chr} ODK form ID.}
\item{form_name}{\code{chr} ODK form display name.}
\item{server_url}{\code{chr} ODK server URL.}
\item{status}{\code{chr} \code{"open"} or \code{"closed"}.}
\item{total_submissions}{\code{int} All-time submission count.}
\item{last_submission_at}{\code{chr} ISO 8601 datetime of most recent submission, or \code{NA}.}
\item{submissions_7d}{\code{int} Submissions in the last 7 days.}
\item{submissions_30d}{\code{int} Submissions in the last 30 days.}
}
}
\description{
Returns submission counts and status metadata for one or more ODK Central
forms. The scope is determined by the combination of \code{project_id} and
\code{form_id} supplied:
}
\details{
\itemize{
\item Both \code{NULL}: all forms across every project visible to the connection.
\item \code{project_id} only: all forms within that project.
\item Both supplied: a single form.
}

Submission counts for the 7-day and 30-day windows are derived by fetching
the full submission list and filtering by \code{createdAt}.
}
