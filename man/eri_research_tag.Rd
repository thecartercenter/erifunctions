% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/research.R
\name{eri_research_tag}
\alias{eri_research_tag}
\title{Tag a reproducible, citable version of a research project}
\usage{
eri_research_tag(
  label,
  description = NULL,
  snapshot = NULL,
  path = getwd(),
  data_con = NULL
)
}
\arguments{
\item{label}{\code{chr} Short, unique tag name (e.g. \code{"lancet-2026-submission"}).}

\item{description}{\code{chr} or \code{NULL} Optional note describing this version.}

\item{snapshot}{\code{chr} or \code{NULL} Which snapshot to bind: a snapshot label or
timestamp already in \code{research.yaml}. If \code{NULL}, the most recent snapshot is
used, or a fresh one is created if none exist.}

\item{path}{\code{chr} Local project root (must contain \code{research.yaml}). Defaults to \code{getwd()}.}

\item{data_con}{Azure container object for the \verb{data/} blob. If \code{NULL}, connects automatically.}
}
\value{
The Azure path of the tag file (invisibly).
}
\description{
Binds a frozen data snapshot, the analysis code commit (the research project's
git SHA), the recorded input provenance, and the output manifest into a single
immutable \strong{tag} at \verb{research/\{project_name\}/tags/\{label\}/_tag.yaml} in the
\verb{data/} Azure blob, and records it in \code{research.yaml}.
}
\details{
A tag answers "what produced this published result?" -- which data
(\code{\link[=eri_research_snapshot]{eri_research_snapshot()}}), which code (git commit), which inputs
(\code{\link[=eri_research_pull]{eri_research_pull()}} / \code{\link[=eri_artifact_pull]{eri_artifact_pull()}} provenance), and which outputs.
Because data is bound by a snapshot and code by a commit SHA, a tagged analysis
can be reproduced from a citation -- including across data updates, by tagging
again after re-pulling refreshed data.

If no snapshot exists yet, one is created automatically. Tags are immutable:
tagging an already-used label is an error.
}
\examples{
\dontrun{
eri_research_snapshot(label = "final-data")
eri_research_tag("lancet-2026-submission", description = "Figures 1-3, Table 2")
}
}
