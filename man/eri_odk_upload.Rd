% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/odk_upload.R
\name{eri_odk_upload}
\alias{eri_odk_upload}
\title{Bulk-create ODK Central submissions from a tabular extract}
\usage{
eri_odk_upload(
  data,
  project_id,
  form_id,
  con = NULL,
  url = Sys.getenv("ODK_URL"),
  auth = Sys.getenv("ODK_TOKEN"),
  key_col = NULL,
  mapping = NULL,
  dry_run = FALSE,
  data_con = NULL
)
}
\arguments{
\item{data}{A file path (CSV/Excel, flat forms only), a data.frame, or a
\strong{named list} of tables (parent first, then \code{"{form_id}-{repeat}"} child
tables) -- the \code{\link[=download_odk_form]{download_odk_form()}}\code{(tables = TRUE)} shape.}

\item{project_id}{\code{int} ODK Central project ID.}

\item{form_id}{\code{chr} ODK Central form ID (xmlFormId); the form must be published.}

\item{con}{An \code{odk_connection} from \code{\link[=init_odk_connection]{init_odk_connection()}}, or \code{NULL} to use
the \code{ODK_URL} / \code{ODK_TOKEN} environment variables.}

\item{url, auth}{\code{chr} Server URL / bearer token, used when \code{con = NULL}.}

\item{key_col}{\code{chr} Column name(s) whose values seed the deterministic
\code{instanceID}. \code{NULL} (default) hashes the whole parent row. To preserve the
original submission identity on a round-trip, pass the id column (e.g. \code{"KEY"}).
Names refer to the columns \emph{after} any \code{mapping} is applied.}

\item{mapping}{Named character vector mapping \strong{input column headers to
field-column names}, for extracts whose headers don't already match the
form (e.g. a paper CSV): \code{c(village = "site_name", date_seen = "visit-date")}.
Targets use the same \code{download_odk_form()} flattening (\code{group-field}). The
rename is applied to the parent and every child table before validation, so
columns you don't list are left as-is. \code{NULL} (default) maps nothing.}

\item{dry_run}{\code{lgl} If \code{TRUE}, run validation only and POST nothing; returns
the validation-issue tibble.}

\item{data_con}{Azure container for optional operation logging; \code{NULL} skips it.}
}
\value{
Invisibly: when \code{dry_run = TRUE}, the validation tibble
(\code{table}, \code{column}, \code{row}, \code{issue}); otherwise a per-row outcome tibble
(\code{instance_id}, \code{status} in \code{created}/\code{skipped}/\code{failed}, \code{http_status},
\code{message}).
}
\description{
The inverse of \code{\link[=eri_odk_sync]{eri_odk_sync()}} / \code{\link[=download_odk_form]{download_odk_form()}}: take a table of
already-collected records (a paper backfill, a legacy export, or a
\code{\link[=download_odk_form]{download_odk_form()}} result) and create them as \strong{submissions} on an
existing \strong{published} ODK Central form. One submission is POSTed per row.
See ADR-0013 for the design contract.
}
\details{
Columns are matched to form fields \strong{by name}, using the same flattening
\code{\link[=download_odk_form]{download_odk_form()}} emits: a field at \verb{/data/visit/date} is the column
\code{visit-date}; repeat groups are supplied as separate child tables named
\code{"{form_id}-{repeat}"} and linked to the parent by a \code{PARENT_KEY} column
whose value matches the parent row's \code{KEY} (ADR-0010). A
\code{download_odk_form(tables = TRUE)} result is therefore a valid \code{data}
argument -- the download/upload round-trips.

Each submission's \code{meta/instanceID} is derived \strong{deterministically} from
\code{key_col} (or the whole row when \code{key_col} is \code{NULL}), so re-running the same
extract re-derives the same ids and ODK Central rejects the duplicates with
HTTP 409 (reported as \code{skipped}) instead of double-loading.
}
\section{Limitations}{

Attachments cannot be attached at submission creation (an ODK API
constraint) and are out of scope. Choice-list validation is best-effort:
values for fields backed by external/dataset choices are not checked here
and surface as \code{failed} rows at POST time if invalid. Submission XML is
built without an instance namespace, matching XLSForm-generated forms.
}

\examples{
\dontrun{
con <- init_odk_connection()

# Round-trip: pull, correct locally, push back.
tabs <- download_odk_form(con = con, project_id = 7,
                          form_id = "RiverProspection", tables = TRUE)

# Preview validation without sending anything.
eri_odk_upload(tabs, project_id = 7, form_id = "RiverProspection",
               con = con, key_col = "KEY", dry_run = TRUE)

# Create the submissions (re-runs skip already-present rows via HTTP 409).
eri_odk_upload(tabs, project_id = 7, form_id = "RiverProspection",
               con = con, key_col = "KEY")

# A paper CSV whose headers don't match the form: map them.
paper <- read.csv("historical_records.csv")   # cols: village, date_seen, stage
eri_odk_upload(paper, project_id = 7, form_id = "RiverProspection", con = con,
               mapping = c(village = "site_name", date_seen = "prospection_date",
                           stage = "river_stage"),
               key_col = "village", dry_run = TRUE)
}
}
