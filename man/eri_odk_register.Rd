% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/odk_registry.R
\name{eri_odk_register}
\alias{eri_odk_register}
\title{Register an ODK form in the shared Azure registry}
\usage{
eri_odk_register(
  project_id,
  form_id,
  country,
  disease,
  server_url,
  form_display_name = NULL,
  con = NULL,
  data_con = NULL
)
}
\arguments{
\item{project_id}{\code{int} ODK Central project ID.}

\item{form_id}{\code{str} ODK Central form ID (xmlFormId).}

\item{country}{\code{str} Country code (e.g. \code{"uga"}). Must be a known ERI country.}

\item{disease}{\code{str} Disease name (e.g. \code{"oncho"}).}

\item{server_url}{\code{str} ODK Central server URL (e.g. \code{"https://odk.example.org"}).}

\item{form_display_name}{\code{str} or \code{NULL} Human-readable form name. Defaults to \code{form_id}.}

\item{con}{\code{odk_connection} or \code{NULL} ODK connection from \code{\link[=init_odk_connection]{init_odk_connection()}}.
Not used for registry writes, but included for consistency with other ODK functions.}

\item{data_con}{Azure container object for the \verb{data/} blob. If \code{NULL}, connects automatically.}
}
\value{
The new registry entry (invisibly).
}
\description{
Appends a new entry to \code{odk/registry.yaml} in the \verb{data/} Azure blob.
Errors if the \verb{(server_url, project_id, form_id)} triple is already active.
}
\examples{
\dontrun{
eri_odk_register(
  project_id = 7, form_id = "RiverProspection",
  country = "uga", disease = "oncho",
  server_url = "https://odk.example.org"
)
}
}
