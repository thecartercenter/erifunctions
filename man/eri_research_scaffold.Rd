% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/research.R
\name{eri_research_scaffold}
\alias{eri_research_scaffold}
\title{Scaffold a new research-project repository}
\usage{
eri_research_scaffold(
  name,
  country,
  disease,
  description,
  dest = getwd(),
  data_con = NULL
)
}
\arguments{
\item{name}{\code{chr} Project name; also the new directory name (e.g. \code{"dr_irs_2024"}).}

\item{country}{\code{chr} Country code (e.g. \code{"dr"}).}

\item{disease}{\code{chr} Disease name (e.g. \code{"malaria"}).}

\item{description}{\code{chr} One-line description of the research question.}

\item{dest}{\code{chr} Parent directory in which to create \verb{name/}. Defaults to \code{getwd()}.}

\item{data_con}{Azure container object for the \verb{data/} blob. If \code{NULL}, connects automatically.}
}
\value{
Path to the created project directory (invisibly).
}
\description{
Creates a standalone analysis-project skeleton (ADR-0006) at \verb{dest/name/}: a README,
an \verb{analysis/} directory seeded with the research-workflow template, a data-safe
\code{.gitignore}, a minimal reproducibility CI workflow, and the standard research scaffold
(\verb{data/}, \verb{figs/}, \verb{outputs/}, \code{research.yaml}) via \code{\link[=eri_research_init]{eri_research_init()}}.
}
\details{
Each research project is its own git repository that depends on \code{erifunctions} -- analysis
code does not live in the package. After scaffolding, the analyst initialises version
control and \code{renv} (see the generated README), sources data with provenance
(\code{\link[=eri_research_pull]{eri_research_pull()}} / \code{\link[=eri_spatial_load]{eri_spatial_load()}}), and freezes citable versions with
\code{\link[=eri_research_tag]{eri_research_tag()}}.
}
\examples{
\dontrun{
eri_research_scaffold(
  "dr_irs_2024", country = "dr", disease = "malaria",
  description = "ITS analysis of IRS impact on malaria incidence in the DR",
  dest = "~/studies"
)
}
}
