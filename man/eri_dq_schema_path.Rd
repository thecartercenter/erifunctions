% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dq.R
\name{eri_dq_schema_path}
\alias{eri_dq_schema_path}
\title{Resolve the local file path of the currently active DQ schema}
\usage{
eri_dq_schema_path(
  country,
  disease,
  data_source = NULL,
  data_type = NULL,
  azcontainer = suppressMessages(get_azure_storage_connection(storage_name =
    Sys.getenv("ERIFUNCTIONS_DATA_STORAGE_NAME", unset = "data")))
)
}
\arguments{
\item{country}{\code{str} Country code (e.g. \code{"dr"}, \code{"uga"}).}

\item{disease}{\code{str} Disease (e.g. \code{"malaria"}, \code{"lf"}).}

\item{data_source}{\code{str} The channel: \code{"surveillance"}, \code{"programmatic"},
\code{"research"}.}

\item{data_type}{\code{str} The measure (e.g. \code{"case"}, \code{"treatment"}); optional
for \code{research}.}

\item{azcontainer}{Azure container object from \code{\link[=get_azure_storage_connection]{get_azure_storage_connection()}}.
Pass \code{NULL} to fork only from the bundled copy.}
}
\value{
\code{str} Local path to the resolved schema file: the override file
itself when a live override exists, a per-user cache copy when the source
is Azure, or the bundled package path when that's the fallback.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Runs the same three-tier resolution as \code{\link[=load_dq_schema]{load_dq_schema()}} (local override ->
Azure -> bundled) but returns the resolved file's local path instead of its
parsed content -- for opening the schema in an editor, or for a script that
wants to know exactly which file will be used without downloading/parsing
it twice.
}
\examples{
\dontrun{
path <- eri_dq_schema_path("atlantis", "oncho", "programmatic", "treatment")
file.edit(path)  # or rstudioapi::navigateToFile(path)
}
}
