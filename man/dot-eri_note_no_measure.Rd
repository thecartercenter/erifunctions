% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dal.R
\name{.eri_note_no_measure}
\alias{.eri_note_no_measure}
\title{Signpost the four-axis (no-measure) approval form}
\usage{
.eri_note_no_measure(data_type)
}
\description{
When \code{eri_approve()} runs without a \code{data_type}, the dataset is filed and
catalogued at the channel level with \code{data_type = NA} (the measure). That is a
legitimate choice for channel-only data (e.g. ODK), but it is indistinguishable
from forgetting the measure, so we say so \strong{once per R session} (guarded by
\code{options(erifunctions.noted_no_measure)}) rather than on every call. No-op when a
measure is supplied.
}
\keyword{internal}
