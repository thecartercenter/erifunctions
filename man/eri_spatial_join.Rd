% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{eri_spatial_join}
\alias{eri_spatial_join}
\title{Join point data to admin boundaries}
\usage{
eri_spatial_join(data, lat_col, lon_col, shapefile, admin_cols = NULL)
}
\arguments{
\item{data}{A data frame or tibble with coordinate columns.}

\item{lat_col}{\code{chr} Name of the latitude column.}

\item{lon_col}{\code{chr} Name of the longitude column.}

\item{shapefile}{An \code{sf} polygon object (e.g. from \code{\link[=eri_spatial_load]{eri_spatial_load()}}).}

\item{admin_cols}{\code{chr} vector of column names to attach from \code{shapefile}.
If \code{NULL} (default), all non-geometry columns are attached.}
}
\value{
A tibble with the original \code{data} columns plus the selected columns
from \code{shapefile}. Geometry is dropped from the result.
}
\description{
Converts a data frame with latitude/longitude columns to an \code{sf} points
object, spatially joins it to a polygon \code{sf} object, and returns the result
as a plain tibble. Rows with \code{NA} coordinates are dropped with a warning.
}
\examples{
\dontrun{
communes  <- eri_spatial_load("ht", level = 2)
case_data <- eri_spatial_join(
  tas_data,
  lat_col    = "lat",
  lon_col    = "lon",
  shapefile  = communes,
  admin_cols = c("adm2_name", "adm1_name")
)
}
}
