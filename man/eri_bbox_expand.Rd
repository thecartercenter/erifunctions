% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{eri_bbox_expand}
\alias{eri_bbox_expand}
\title{Expand a bounding box by a distance in metres}
\usage{
eri_bbox_expand(bbox, X, Y, X2 = X, Y2 = Y, crs_out = 4326)
}
\arguments{
\item{bbox}{A bounding box produced by \code{\link[sf:st_bbox]{sf::st_bbox()}}.}

\item{X}{\code{num} Padding in metres on the west side (and east if \code{X2} is not given).}

\item{Y}{\code{num} Padding in metres on the south side (and north if \code{Y2} is not given).}

\item{X2}{\code{num} Padding in metres on the east side. Defaults to \code{X}.}

\item{Y2}{\code{num} Padding in metres on the north side. Defaults to \code{Y}.}

\item{crs_out}{\code{int} EPSG code for the output CRS. Defaults to \code{4326} (WGS84 lat/lng).}
}
\value{
A bounding box object (\code{bbox}). Convert to an \code{sf} polygon with
\code{\link[sf:st_as_sfc]{sf::st_as_sfc()}}.
}
\description{
Takes an \code{sf} bounding box and expands it by \code{X} metres in the east-west
direction and \code{Y} metres in the north-south direction. Useful for adding
padding around a study area before mapping.
}
\details{
Ported from \code{sirfunctions::f.expand.bbox()} /
basemapR.
}
\examples{
\dontrun{
haiti <- eri_spatial_load("ht", level = 0)
bbox  <- sf::st_bbox(haiti) |> eri_bbox_expand(X = 10000, Y = 10000)
}
}
