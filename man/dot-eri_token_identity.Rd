% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dal.R
\name{.eri_token_identity}
\alias{.eri_token_identity}
\title{Verified actor identity from the connection's Azure AD token}
\usage{
.eri_token_identity(con)
}
\description{
Extracts the authenticated user's identity (\code{upn} / \code{preferred_username}) from
the AzureAuth token carried by an interactive storage connection (ADR-0003).
This is the trustworthy approver identity: unlike \code{ERI_ANALYST_ID}, it cannot
be self-declared. Returns \code{NULL} when there is no user claim (a service-principal
token) or no decodable token (a mock / SAS-based container) — callers then fall
back to \code{ERI_ANALYST_ID}.
}
\keyword{internal}
