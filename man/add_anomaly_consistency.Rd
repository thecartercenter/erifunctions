% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dq.R
\name{add_anomaly_consistency}
\alias{add_anomaly_consistency}
\title{Flag cross-field consistency violations defined in a schema}
\usage{
add_anomaly_consistency(data, schema)
}
\arguments{
\item{data}{A tibble or \code{dq_result} object.}

\item{schema}{Named list from \code{\link[=load_dq_schema]{load_dq_schema()}}.}
}
\value{
A tibble of violations with columns \code{row}, \code{column}, \code{value}, and
\code{issue} (includes the rule name and message). If the input is a \code{dq_result},
violations are appended to \verb{$flags} and the updated \code{dq_result} is returned.
Returns an empty tibble when all rules pass.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Evaluates named consistency rules from the schema's \code{consistency} block and
flags rows where a rule is violated. Each rule specifies a \code{lhs} side, a
comparison \code{op}, and a \code{rhs} side.

Schema format (add a \verb{consistency:} block to any YAML schema):

\if{html}{\out{<div class="sourceCode yaml">}}\preformatted{consistency:
  positives_le_tested:
    lhs: NumMicroPos
    op: "<="
    rhs: NumTestedMicro
    message: "Positive cases exceed tested"
  age_non_negative:
    lhs: Age
    op: ">="
    rhs_value: 0
    message: "Age is negative"
  gender_sum_matches_type_sum:
    lhs_sum: [male_trained, female_trained]
    op: "=="
    rhs_sum: [new_male_jan, new_fem_jan, refresh_male_jan, refresh_fem_jan, "..."]
    message: "Sum of male+female trained does not equal sum of new+refresher trained"
}\if{html}{\out{</div>}}

Each side is exactly one of: a single column (\code{lhs}/\code{rhs}), a constant
(\code{rhs_value}), or a list of columns summed row-wise with \code{na.rm = TRUE}
(\code{lhs_sum}/\code{rhs_sum}) -- for a measure with no single annual roll-up column
to compare against another single column, e.g. a monthly-wide sheet where
the total-by-gender columns should equal the sum of many monthly
new/refresher columns. A row where \emph{every} column on a summed side is \code{NA}
stays \code{NA} (genuinely no data that side can speak to, not a fabricated 0)
and is skipped like any other \code{NA} operand; a row where \emph{some} columns are
present and others \code{NA} treats the missing ones as 0 (a month with nothing
reported legitimately contributes 0 to the sum, same reasoning as
\code{.dq_na_fill()} for a single count column). A summed side where none of its
columns exist in the data at all skips the whole rule for that side (e.g. a
schema shared across several sheet shapes, where only one prefix's columns
are ever populated on a given ingest -- same "not found" skip as a missing
\code{lhs}/\code{rhs}).

Supported operators: \code{<=}, \code{>=}, \code{==}, \code{<}, \code{>}, \code{!=}.
Missing values (\code{NA}) in either operand skip the check for that row.

Works on a plain tibble (returns a tibble of violations) or a \code{dq_result}
(appends violations to \verb{$flags}).
}
\examples{
\dontrun{
schema <- load_dq_schema("haiti", "malaria")
run_dq_checks(data, schema) |> add_anomaly_consistency(schema)
}
}
