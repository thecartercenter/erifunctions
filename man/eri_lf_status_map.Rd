% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi_lf.R
\name{eri_lf_status_map}
\alias{eri_lf_status_map}
\title{LF programme status choropleth map}
\usage{
eri_lf_status_map(
  shapefile,
  status_data,
  eu_col,
  status_col,
  title = NULL,
  scale_bar = TRUE,
  north_arrow = TRUE
)
}
\arguments{
\item{shapefile}{An \code{sf} polygon object.}

\item{status_data}{A data frame with a status column and a join key.}

\item{eu_col}{\code{chr} Column present in BOTH \code{shapefile} and \code{status_data}
used to join them (e.g. \code{"eu"} or \code{"adm2_name"}).}

\item{status_col}{\code{chr} Column in \code{status_data} with LF programme status
values (should match \code{eri_lf_program_levels()} entries).}

\item{title}{\code{chr} Map title. Default \code{NULL}.}

\item{scale_bar}{\code{lgl} Add ggspatial scale bar? Default \code{TRUE}.}

\item{north_arrow}{\code{lgl} Add ggspatial north arrow? Default \code{TRUE}.}
}
\value{
A ggplot object.
}
\description{
Wrapper around \code{eri_map_choropleth()} that applies the standard
\code{"lf.status"} colour scheme and discrete factor levels from
\code{eri_lf_program_levels()}.
}
\details{
Requires \code{ggplot2} (in Imports). \code{ggspatial} (Imports) is used for scale
bar and north arrow by default; set \verb{scale_bar = FALSE, north_arrow = FALSE}
to suppress.
}
\examples{
\dontrun{
eu_sf   <- eri_spatial_load("dr", level = 2)
eri_lf_status_map(eu_sf, lf_status, "adm2_name", "status",
                  title = "DR LF programme status 2024")
}
}
