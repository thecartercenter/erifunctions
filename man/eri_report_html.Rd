% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reports_html.R
\name{eri_report_html}
\alias{eri_report_html}
\title{Render an ERI-branded self-contained HTML report}
\usage{
eri_report_html(
  sections,
  path,
  title = "ERI Report",
  subtitle = NULL,
  author = NULL,
  date = format(Sys.Date(), "\%B \%d, \%Y")
)
}
\arguments{
\item{sections}{Named list of section definitions. Each element must be a
named list with any of:
\itemize{
\item \code{heading} — character; section heading
\item \code{text} — character; narrative paragraph (markdown allowed)
\item \code{table} — data frame to render via \code{\link[=eri_table]{eri_table()}}
\item \code{figure} — a \code{ggplot} object
\item \code{figure_width}, \code{figure_height} — numeric inches (defaults: 8, 5)
}}

\item{path}{Character; output file path (should end in \code{.html}).}

\item{title}{Character; report title displayed in the header.}

\item{subtitle}{Optional character; subtitle displayed below the title.}

\item{author}{Optional character; displayed in the report header.}

\item{date}{Optional character; defaults to today's date.}
}
\value{
\code{path} invisibly.
}
\description{
Renders a Quarto template to a single portable \code{.html} file. No external
files are produced — the output is self-contained and can be emailed or
shared directly.
}
\details{
The report is structured as a series of sections, each optionally containing
a heading, free text, a formatted table (via \code{\link[=eri_table]{eri_table()}}), and/or a
ggplot figure.

Requires the \code{quarto} package and a working Quarto installation
(\url{https://quarto.org}).
}
\examples{
\dontrun{
eri_report_html(
  sections = list(
    overview = list(
      heading = "Case summary",
      text    = "Cases increased in Q3.",
      table   = summary_df
    ),
    trends = list(
      heading = "Epidemic curve",
      figure  = epicurve_plot
    )
  ),
  path     = "outputs/malaria_report.html",
  title    = "Hispaniola Malaria 2024"
)
}
}
