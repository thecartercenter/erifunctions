% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{eri_spatial_promote}
\alias{eri_spatial_promote}
\title{Promote a research-project boundary to the canonical \verb{/spatial} store}
\usage{
eri_spatial_promote(
  local_path,
  country,
  level,
  overwrite = FALSE,
  path = getwd(),
  data_con = NULL
)
}
\arguments{
\item{local_path}{\code{chr} Path to the local boundary file to promote (typically a cleaned copy
under the project \verb{data/} directory).}

\item{country}{\code{chr} Country code (e.g. \code{"dr"}, \code{"ht"}).}

\item{level}{\code{int} Admin level (0–4).}

\item{overwrite}{\code{lgl} If \code{TRUE}, replace an existing canonical boundary. Default \code{FALSE}.}

\item{path}{\code{chr} Local project root (read for \code{research.yaml} to record provenance). Defaults
to \code{getwd()}. If no \code{research.yaml} is found, the promotion proceeds but is not recorded
(with a warning).}

\item{data_con}{Azure container object for the \verb{data/} blob. If \code{NULL}, connects automatically.}
}
\value{
The canonical Azure blob path (invisibly).
}
\description{
The explicit gate for pushing a boundary you have cleaned in a research project up to the
shared canonical \verb{data/spatial/\{country\}/adm\{level\}.rds}, where other users and studies pull
it. Unlike \code{\link[=eri_spatial_upload]{eri_spatial_upload()}} (for brand-new boundaries), \code{eri_spatial_promote()} is the
deliberate way to \emph{replace} an existing canonical boundary, and it records the promotion in
the project's \code{research.yaml} for provenance. Replacing an existing boundary still requires an
explicit \code{overwrite = TRUE} so shared data is never clobbered by accident. See ADR-0009.
}
\details{
The boundary is validated exactly as in \code{\link[=eri_spatial_upload]{eri_spatial_upload()}} before promotion.
}
\examples{
\dontrun{
# After cleaning a boundary inside a research project, promote it to canonical.
eri_spatial_promote("data/dr_adm3_cleaned.rds", country = "dr", level = 3, overwrite = TRUE)
}
}
